# 도구 조사 기록 (자가개선 루프가 회차마다 덧붙인다)

규칙: **무료이고 키 없이 되는 것만.** 유료는 적지 않는다.
실제로 깔고 한 번 돌려 본 것만 "된다"고 적는다. 안 되면 왜 안 되는지 적는다.

## 2026-09-23 20:10 — TTS: kokoro-onnx → **우리에게는 못 씀(한국어 없음)**
- 왜 찾았나: 쇼츠 내레이션은 지금 edge-tts(`ko-KR-SunHiNeural`)로 마이크로소프트 서버에 붙는다.
  키는 없지만 남의 서버라 막히면 쇼츠 생산이 통째로 멈춘다. 오프라인 대체를 찾았다.
- 한 것: `pip install kokoro-onnx` 실제 설치 성공. 패키지 안의 언어 코드를 직접 뒤졌다.
- **결과: 지원 언어가 `en-us`, `en-gb` 둘뿐. 한국어(ko) 없음.** 검색 결과 여러 곳이
  "다국어 지원"이라고 썼지만 실제 패키지에는 한국어가 없었다 — 그래서 설치해 보고 확인한 것이다.
- 조치: 지웠다(`pip uninstall kokoro-onnx`). edge-tts를 계속 쓴다.
- 남는 일: 한국어 되는 오프라인 TTS는 아직 못 찾았다. 다음 회차에 다른 후보를 본다.

## 2026-09-23 20:08 — 자막: Subtitle Edit 5.1.0 / Aegisub / Gaupol → **안 깖(우리 구조에 안 맞음)**
- 전부 무료·오픈소스이고 한국어 UI도 있지만 **사람이 눌러 쓰는 GUI 편집기**다.
- 우리 쇼츠는 shorts.py가 자막 글자를 직접 그려 넣는다(사람 손 없음). 무인 발행 구조에
  GUI 편집기는 붙일 자리가 없다. 그래서 설치하지 않았다 — 못 쓴다는 게 아니라 필요가 없다.

## 2026-09-24 10시 회차 — 무료·키 없음, 실제로 돌려 본 것만

- **Fotocasa(스페인 매물)** — 무료, 키 없음, 브라우저도 필요 없음. 목록 페이지 HTML 안에
  매물 JSON이 그대로 박혀 있어 `requests` 한 번으로 읽힌다. 실제로 돌려 확인:
  마드리드 6건·바르셀로나 3건(월세·면적·방·동네·URL·게시일). work/listings.py 에 `fotocasa` 출처로 붙였다.
  `py -3.12 work/listings.py 마드리드 6` · 발렌시아도 같은 주소 규칙.
  ※ 같은 날 idealista.com(스페인)은 403, Rumah123(인도네시아)도 403 — 발리는 아직 출처가 없다.
- **금감원 finlife(정기예금)** — 키 있음(무료). 다시 확인: 우리은행 WON플러스예금 1개월 2.7% /
  3개월 2.9% / 6개월 3.3%, 공시월 202609. 은행연합회 소비자포털에는 외화예금 금리 공시가 없다(확인함).
- 이번 회차에는 WebSearch 로 새 오픈소스를 훑는 일은 **못 했다** — 루프 판정 버그와 숏폼 화면 겹침을
  고치는 데 시간을 다 썼다. 다음 도구 회차로 넘긴다.

## 2026-09-25 04:1x (loop 회차) — 한국어 오프라인 TTS: MeloTTS → **이 PC에는 못 깖(빌드 도구 없음)**
- 왜 찾았나: 2026-09-23에 kokoro-onnx 가 한국어 없어서 접고 "다음 회차에 다른 후보를 본다"고 남겨 둔 숙제다.
  쇼츠 내레이션은 지금도 edge-tts(`ko-KR-SunHiNeural`)로 **마이크로소프트 서버**에 붙는다. 키는 없지만
  남의 서버라 막히면 쇼츠 생산이 통째로 멈춘다.
- 후보: **MeloTTS**(myshell-ai, MIT, CPU로 돌아감, 한국어 모델 `MeloTTS-Korean` 있음). 무료·키 없음.
- 실제로 해 본 것 두 가지 — **둘 다 실패**:
  1. `pip install melotts` → PyPI sdist 가 깨져 있다. 빌드 중
     `FileNotFoundError: ... melotts_.../requirements.txt` 로 죽는다(패키지 쪽 문제, 우리 환경 문제 아님).
  2. `pip install git+https://github.com/myshell-ai/MeloTTS.git` → 본체(melotts)와 의존 15개는 빌드에 성공했는데
     **fugashi, tokenizers 두 개가 휠 빌드에 실패**한다(`failed-wheel-build-for-install`).
     둘 다 C++/Rust 컴파일이 필요한데 이 PC에 빌드 도구가 없다. `import melo` → ModuleNotFoundError.
- 결론: **edge-tts 를 계속 쓴다.** MeloTTS 자체가 안 되는 게 아니라 이 PC에 컴파일러가 없어서 못 깐다.
- 다음에 누가 이어받을 때: (a) Visual Studio Build Tools + Rust 를 깔면 될 가능성이 있다(사장님 결정 필요,
  설치 용량이 크다), 또는 (b) fugashi/tokenizers 의 미리 빌드된 휠이 py3.12 windows 용으로 올라왔는지 다시 본다.
  fugashi 는 일본어 형태소 분석기라 **한국어에는 안 쓰일 수도 있다** — 의존에서 뺄 수 있는지 보는 게 가장 빠른 길이다.
- 아직 못 한 것: 한국어 되는 오프라인 TTS를 실제로 돌려 본 적은 여전히 없다. 그렇게 적어 둔다.

## 2026-09-26 06:0x (loop 회차) — 말에 맞춰 자막 띄우기: **faster-whisper 깔았고 실제로 돌아간다**

**왜 찾았나.** 우리 쇼츠는 장면마다 head·lines 만 박혀 있고, 읽어 주는 말(say)에 맞춰
글자가 따라 뜨지 않는다. 경쟁 쇼츠는 말에 맞춰 단어가 하나씩 켜진다.
edge-tts 는 mp3 만 주고 어느 단어가 몇 초에 나오는지는 안 알려 준다.

**깐 것.** `py -3.12 -m pip install faster-whisper` → 1.2.1. 무료·오픈소스·키 없음·오프라인.
모델은 처음 돌릴 때 자동으로 받는다(tiny, int8, CPU).

**실제로 돌린 것.** 방금 만든 숏폼의 2번 장면 음성(a01.mp3, 8초)에 걸었다.
- 모델 로딩+받기 포함 11.5초 · 언어 ko 자동 인식 · 구간 2개 · `word_timestamps=True` 로 단어별 시각까지 나온다
- 받아쓰기 자체는 틀린다: "전세가율" → "전세가 유리", "관악구도" → "관하구도", "58.66%" → "58.6%"
- **그래도 쓸 수 있다.** 우리는 말한 글자(say)를 이미 알고 있다. 필요한 건 받아쓰기가 아니라 **시각**이다.
  단어 순서대로 우리 원문에 붙이면(정렬) tiny 모델로도 자막 타이밍이 나온다. 큰 모델을 받을 이유가 없다.

**다음에 쓸 자리.** shorts.py 의 장면을 mp3 길이만큼 정지 화면으로 늘리는 대신,
say 를 단어 단위로 잘라 시각에 맞춰 켜는 자막 띠를 붙인다. ffmpeg drawtext 나 PNG 여러 장으로 된다.
지금 판형(글자 위·자료 아래)을 안 건드리고 아래쪽 빈 자리에만 넣으면 자료도 안 가린다.

**주의.** 첫 실행에서 huggingface 캐시가 심볼릭 링크 경고를 낸다(Windows 개발자 모드 꺼짐).
동작에는 문제 없고 디스크만 조금 더 쓴다. 고치려면 개발자 모드를 켜야 하는데 사장님 손이 필요해서 안 건드렸다.
