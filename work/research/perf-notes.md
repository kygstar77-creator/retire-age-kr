# 성과 메모 (자가개선 루프가 회차마다 한 단락씩 덧붙인다)

## 2026-09-23 20:00 (루프 19회차)
측정한 것만 적는다. 하루치로 단정하지 않는다.

- **블로그 색인**: 오늘 발행 12편 중 색인 5편(42%). 어제 8/12(67%), 그제 3/6(50%).
  오늘치는 발행한 지 몇 시간 안 된 글이 섞여 있어 아직 덜 잡힌 것일 수 있다 —
  내일 같은 글을 다시 재서 5/12가 그대로면 그때 문제로 본다.
- **제목 검색 순위**: 12편 중 10위 밖 2편(건강보험료율·양도세계산기). 나머지는 1~7위.
  두 편 다 계산기·요율처럼 경쟁이 센 말이 제목 앞에 있다. 표본 2편이라 규칙으로 올리지 않는다.
- **카페 조회**: 최근 30편 중앙 2회(어제 25편 중앙 3, 그제 23편 중앙 3) — 중앙값은 내렸고
  평균은 6.7 → 9.1, 최고는 51 → 104로 올랐다. 잘 되는 한 편이 평균을 끌어올린 모양새지
  전체가 오른 게 아니다. 카페 규모가 작아 중앙값은 계속 낮게 나온다.
- **방문자**: 오늘 57명 / 누적 11,102명. visitors_log에 오늘 것만 있어 어제 대비는 못 잰다
  (기록이 오늘 시작됐다) — 내일부터 비교 가능.
- **영상**: 총조회 3,518, 구독 39. 오늘 올린 쇼츠 4편은 0·1·0·0회로 아직 판단 불가.
  6월 영상(1,969 / 1,129회)이 총조회의 대부분이라 쇼츠 성과는 아직 신호가 없다.

판정 보류: 오늘 바꾼 썸네일 노랑 비율(yellow_frac)은 아직 이 숫자들에 반영되지 않았다.
지금 올라간 쇼츠들은 전부 바꾸기 전 썸네일이다.

## 2026-09-23 22:00 (루프 27회차)
측정한 것만 적는다. 하루치로 단정하지 않는다.

- **블로그 색인**: 오늘 발행 12편 중 색인 8편(67%). 20시 회차에서 같은 12편이 5편(42%)이었으니
  두 시간 사이 3편이 더 잡혔다. 20시 메모에서 "발행한 지 몇 시간 안 돼 덜 잡힌 것일 수 있다"고
  보류했던 쪽이 맞았다 — 색인은 발행 직후가 아니라 몇 시간에 걸쳐 올라온다.
  아직 안 잡힌 4편: 코픽스 9월 공시 / 10월 배당 일정 / 근로장려금 2026 / 건강보험료율 2026.
- **제목 검색 순위**: 12편 중 10위 밖 1편(건강보험료율 2026). 20시에는 2편(건강보험료율·양도세계산기)
  이었다. 건강보험료율은 두 회차 연속 30위 밖 + 색인 안 됨으로 유일하게 양쪽 다 막힌 글이다.
  표본 1편이라 아직 규칙으로 올리지 않는다. 내일 같은 글이 그대로면 제목을 본다.
- **방문자**: 오늘 62명 / 누적 11,107명. 20시 57명 → 22시 62명, 두 시간에 +5.
  어제치 기록이 없어 전일 대비는 여전히 못 잰다.
- **영상**: 총조회 3,531, 구독 39. 20시 3,518 → 3,531(+13). 오늘 올린 쇼츠 4편은 0·1·0·1회로
  거의 움직이지 않았다. 증가분은 6월 영상 쪽으로 보인다(쇼츠 합이 2회라 +13을 설명 못 함).
  쇼츠는 4편·이틀치라 성과 신호로 읽지 않는다.

이번 회차에 고친 것은 성과가 아니라 **자(measure)**다. 쇼츠 썸네일 샘플을 배경 없이 그려 재던 것을
실제 판형(자료 화면 위에 글자)으로 바꿨다. 지금 올라간 쇼츠들은 전부 그 전 썸네일이라
이 변경의 효과는 다음에 올리는 영상부터 나타난다 — 오늘 숫자로 판정하지 않는다.

이번 회차 숏폼 1편 생산: 서울 아파트 월세 수익률(금천 2.40% / 강남 1.03% / 종로 0.95% 최저).
대본은 내가 직접 썼고(제미나이 안 씀) shortscript check 통과. 장면 4개 전부 chartimg 막대차트를 깔았다.
올림: https://youtu.be/zHnqdIdQhHk (private)
고친 것 하나 — 4번 장면에 "나머지는 전부 1%대 아래"라고 썼다가 종로 0.95%가 1% 아래라 틀린 말이라
"나머지는 모두 2% 아래"로 바꿨다. 검사기는 숫자만 보지 이런 말은 못 잡는다.

## 2026-09-24 00:2x — 루프 34·35회차 (디자인 차이 6 → 3)
- 우리 영상 조회 합계(최근 10편) 3304 → 3304, 변화 없음. 하루치라 단정하지 않는다.
- 썸네일 판형: 쇼츠 세 칸 오차 합 0.7769 → 0.6046 (실측). short.text_spread 0.6304 → 0.15.
  닫힌 차이 short.text_mid·short.text_top·short.yellow / 남은 차이 short.white·short.text_bot·long.text_top.
- short.white 는 이번 회차에도 못 닫았다. 실측해 보니 stroke_ratio(6~40)·panel_alpha(60~230)·text_y·text_spread
  어느 손잡이도 white 를 0.0214~0.0224 밖으로 움직이지 못한다. 외곽선은 흰 글자 뒤에 그려져 흰 픽셀을 깎지 않는다.
  text_scale 은 쇼츠 하한 0.9 에 붙어 있다(글자량을 지키려고 올려 둔 하한). 세 회차째 막힘 → tools-wanted.md 로 올림.
- 숏폼 1편 생산·업로드(비공개): https://youtu.be/wIE0NIE_kvA — "JEPI vs JEPQ 1년, 100주 분배금 62만원 vs 92만원".
  대본은 클로드가 직접 썼고 shortscript check 통과. 장면 4개 전부 chartimg 막대 차트를 자료 화면으로 깔았다
  (글자만 있는 장면 없음). 사실은 work/research/jepiq/facts.txt(야후 파이낸스 chart API + JP모건 공식 팩트시트)만 썼다.

## 2026-09-24 02:13 (loop step39~42)
영상: 채널 누적 조회 3,518 · 구독 39 · 영상 6편(스탯 기준). 최근 10편 조회 합계는 3,266 으로 지난 회차와 같다 —
어제 올린 쇼츠 6편이 전부 0~3회라 아직 판정할 표본이 아니다. 오늘 7편째(전세가율 단지별)를 올렸다.
블로그: 12편 중 색인 10편(0.833) — 지난 회차 계기판의 0.667 보다 올랐다. 제목 검색 10위 밖은 2편.
색인 안 됨 2편은 코픽스 9월 공시, 10월 배당 일정 — 둘 다 어제 발행분이라 시간 문제일 수 있다(단정하지 않음).
방문자: 오늘 12 · 전체 11,129(어제 대비 +22). 오늘 수치는 새벽 02시라 하루치로 못 본다.
썸네일: short.white 0.0224 → 0.0076(목표 0.0107) — 새 손잡이 text_tint 로 두 회차 막힘이 풀렸다.
남은 차이 셋: short.text_bot 0.1898→0.3093 · short.yellow 0.0086→0.0067 · long.text_top 0.6787→0.9241.


## 2026-09-24 04시 루프 회차
- 영상: 채널 구독 39 · 영상 6 · 총조회 3,525. 지난 회차 이후 최근 10편 조회 합계 2,137 → 2,137(그대로).
  어제 올린 쇼츠 7편은 0~3회에 머문다. 예전 롱폼 QQQM·SCHD 1,969회가 총조회의 대부분이다.
  하루치로는 판단하지 않는다 — 쇼츠는 올린 지 하루도 안 됐고 전부 비공개(private)로 올라가 있다.
  ※ 비공개면 조회가 안 늘어나는 게 당연하다. 공개 전환은 사장님 판단이 필요하다(routine이 공개로 바꾸지 않는다).
- 블로그 방문: 오늘 17 · 전체 11,134(전체 어제보다 +27). 03:54 계기판의 오늘 12에서 17로 늘었다.
- 블로그 색인·순위(최근 12편): 제목검색 1위 7편 · 9위 1편 · 30위 밖 3편, 색인 안 됨 3편.
  색인 안 된 3편 중 2편은 오늘 새벽 발행분(상계주공9단지·DSR 한도)이라 아직 이른 것으로 보인다.
  코픽스 글은 1위인데 색인 안 됨으로 나온다 — 제목으로는 잡히고 본문 문장으로는 안 잡히는 경우다.
- 이번 회차 숏폼 1편 생산·업로드: https://youtu.be/EQQPfhDiRt0 (비공개)
  '서울 오피스텔 수익률, 답십리 10.00% 목동 1.02% (단지별 실거래)'. 대본은 클로드가 직접 썼고
  shortscript check 통과. 장면 4개 전부 chartimg 자료 화면을 깔았다(글자만 있는 장면 없음).

## 2026-09-24 06시 루프 회차
- 영상: 채널 구독 39 · 영상 6 · 총조회 3,525 — 2시간 전과 한 자리도 안 움직였다.
  최근 10편 조회 합계 2,089 → 2,089. 어제 올린 쇼츠 8편은 여전히 0~3회다.
  전부 비공개(private)라 조회가 안 늘어나는 게 당연하다. 공개 전환은 사장님 판단이 필요하다.
- 블로그 방문: 오늘 21 · 전체 11,138(전체 어제보다 +31). 04시 회차의 오늘 17 → 21로 늘었다.
  하루 목표 100까지는 멀다. 발행량이 아니라 색인·순위가 먼저라는 판단은 그대로다.
- 블로그 색인·순위(최근 12편): 제목검색 1위 5편 · 7위 1편 · 30위 밖 5편, 색인 안 됨 4편.
  04시 회차(1위 7편 / 30위 밖 3편 / 색인 안 됨 3편)보다 내려간 것처럼 보이지만, 그 사이 새 글
  4편이 목록 앞으로 들어와 오래된 상위 글을 12편 밖으로 밀어낸 탓이다. 같은 글끼리 견준 게 아니라서
  '순위가 떨어졌다'고 읽으면 안 된다. 색인 안 된 4편은 전부 오늘 새벽 발행분이다.
- 카페: 최근 30편 조회 중앙값 1회 · 최고 123회. 회원 수가 적은 카페라 이 수치만으로는 글 품질을
  판단할 수 없다는 기존 단서 그대로.
- 이번 회차 숏폼 1편 생산·업로드: https://youtu.be/j1k9eruoig0 (비공개)
  '서울 오피스텔, 아파트값의 20.6% 강남과 71.4% 중랑 (9월 실거래)'. 대본은 클로드가 직접 썼고
  shortscript check 한 번에 통과. 장면 5개 전부 chartimg 막대 차트를 깔았다(글자만 있는 장면 없음).
  재료는 work/research/offiseoul/facts.txt — 국토부 실거래 9월 계약분 오피스텔 444건·아파트 1,235건.

## 2026-09-24 08시 회차
- 블로그 방문: 오늘 26 · 전체 11,143(어제 대비 +36). 07:53 계기판의 21보다 조금 올랐으나 하루치라 단정하지 않는다.
- 색인·순위(최근 12편): 제목 검색 10위 밖 7/12편, 본문 문장으로도 안 잡히는 '색인 안 됨' 6편.
  오늘 아침 발행분 4편이 전부 색인 안 됨인데, 발행 직후라 아직 판단할 수 없다(어제 발행분은 대부분 1~5위로 잡혔다).
  1위로 잡힌 것: 오피스텔/아파트값 비율, 레나-버크셔, TIGER 미국S&P500, TQQQ. 5위: 월급계산기 2026.
- 유튜브: 구독 39 · 총조회 3,525. 어제 올린 쇼츠 9편은 0~3회로 사실상 안 돌고 있다(비공개 업로드라 당연).
  지난 회차 대비 최근 10편 조회 합계 120 → 120 으로 변화 없음.
- 숏폼 1편 생산·업로드(비공개): "한 달 3% 준다는 커버드콜, 9월 공시 1위는 1.94%" https://youtu.be/9wvGhoVw3hw
  대본은 내가 직접 썼고(제미나이 안 씀) shortscript check 통과. 장면 5개 전부 chartimg 막대 차트를 깔았다.
  이번 회차에 고친 썸네일 손잡이(yellow_tint·tint_v)가 실제 제작물에 처음 들어간 편이다 — 차트가 가려지지 않고 보인다.

## 2026-09-24 10시 회차
- 블로그 방문: 오늘 34 · 전체 11,151(어제 대비 +44). 08시 회차의 오늘 26·전체 11,143보다 둘 다 올랐다. 하루치라 추세로 단정하지 않는다.
- 색인·순위(최근 12편): 제목 검색 10위 밖 8/12편, '색인 안 됨'(본문 문장으로도 안 잡힘) 7편.
  08시(10위 밖 7 · 색인 안 됨 6)보다 각각 1편씩 나빠졌는데, 오늘 아침 발행분이 더 늘어 아직 색인 전인 것이라
  글 품질 판단으로 쓰지 않는다. 1위로 잡힌 것: 오피스텔/아파트값 비율, 레나-버크셔, TIGER 미국S&P500. 5위: 월급계산기 2026.
- 카페 최근 30편 조회 중앙값 1회, 최고 33회. 비교 기준(회원 많은 파이어족 카페 600편 중앙값 248회)과는 카페 규모 차이가 크다.
- 유튜브: 구독 39 · 총조회 3,525(08시와 같음). 최근 10편이 **전부 private** 임을 이번에 처음 수치로 확인했다
  (ytupload stats 에 공개상태를 찍게 고쳤다). 비공개면 조회가 영영 0이므로 조회로는 썸네일 조정을 판정할 수 없다.
- **이번 회차에 찾은 것**: 디자인 루프가 '최신 10편 조회 합계'로 조정 효과를 판정하고 있었다.
  새 영상을 올리면 그 합계가 무너진다(실측 2089 → 120 → 6, 이번 업로드 뒤 6 → 5).
  그래서 발행할 때마다 "조회 하락"으로 읽혀 직전 회차의 디자인 조정을 전부 되돌렸다.
  short.contrast·short.white 가 여러 회차 동안 닫히지 않은 진짜 원인이 이것이다(64 조정 → 65 되돌림 → 66 재조정 → 67 닫힘).
  판정을 누적 총조회의 시간당 증가로 바꾸고, 늘어난 조회가 30회 미만이면 판정을 보류하게 했다.
  69회차에서 확인: 최신 10편 합계는 6→5로 떨어졌지만(옛 규칙이면 되돌림 발동) 새 규칙은 판정 보류,
  tint_v 0.75 · text_tint 0.9014 그대로 유지. 남은 차이 0개(67·68·69 연속).
- 숏폼 1편 생산·업로드(비공개): "주담대 40년 만기, 늘어난 한도 3,227만원과 늘어난 이자 16,773만원"
  https://youtu.be/eKMSLnBKYLU · 39초 · 장면 5개 전부 chartimg 막대 차트.
  대본은 내가 직접 썼고(제미나이 안 씀) shortscript check 통과.
- **화면 검증에서 찾은 것 2가지**(식 검증으로는 안 잡혔다):
  (1) 숏폼 장면이 자료 화면을 화면 전체에 깔고 그 위에 글자를 얹고 있었다. 차트가 y 339~1581 에 들어가는데
      글자도 y 300 부터 시작해 차트 제목·값이 머리글 뒤에 그대로 겹쳤고, 겹침을 가리려 화면을 0.42로 눌러
      막대가 아예 안 보였다. 글자를 위에, 자료 화면을 그 아래 남은 자리에 통째로 넣도록 고쳤다(밝기 0.82).
  (2) chartimg 가 제목을 26자에서 무조건 잘라 "…5,000만원  5.2" 처럼 조건이 끊겨 나갔다. 폭에 맞게 글자를 줄이도록 고쳤다.
- **다음 회차로 넘기는 것**: 썸네일(thumb.py)은 아직 같은 겹침이 남아 있다. 차트를 화면 전체에 깔고 글자를 얹어
  차트의 큰 글자('30년 한도', '33,409만원')가 머리글과 부딪친다. 다만 thumb.py 의 text_y·text_spread 는 루프가
  실측으로 맞춰 둔 값이라, 배치를 바꾸면 그 값들이 무효가 된다. 배치 변경과 손잡이 재측정을 같이 해야 한다.

## 2026-09-24 12:20 (자가개선 루프 70·71회차)
- 블로그 방문: 오늘 41 · 전체 11,158(어제보다 +51). 10:22 회차에는 오늘 34였다 → **1.7시간에 +7**.
  올랐다. 다만 오전 시간대라 하루치로 단정하지 않는다.
- 블로그 12편 점검: 제목 검색 1위 2편(오피스텔/아파트값 비율, 레나-버크셔), 10위 밖 10편, 색인 안 됨 9편.
  9편이 색인 안 된 것은 오늘 새벽~오전에 올라간 글이라 아직 색인 전일 수 있다 — 내림으로 읽지 않는다.
- 카페 최근 30편 조회 중앙값 1회, 최고 33회(지난 회차와 같음).
- 유튜브: 구독 39 · 총조회 3,527(10:22 3,525 → **+2**). 최근 10편 **전부 private 그대로**.
- **이번 회차에 고친 것 (루프가 못 닫는 차이)**: 70회차 gaps 는 0개였다(썸네일 수치는 경쟁 25% 안쪽).
  루프가 못 닫고 있던 것은 차이가 아니라 **판정**이다. 두 군데를 고쳤다.
  (1) *버그*: 공개 0편이어도 `gain >= 30` 만 넘으면 되돌리기 가지가 발동할 수 있었다.
      최근 10편이 전부 비공개면 늘어난 채널 총조회는 **옛 공개 영상**이 번 것이라 이번 회차 손잡이와 무관한데,
      그 수치로 직전 조정을 되돌릴 수 있었다는 뜻이다. 공개 0편이면 조회를 판정에 쓰지 않게 막았다.
  (2) *판정 눈금 추가*: 조회로 판정할 수 없는 회차에도 숫자가 남게, 매 회차 재는 **경쟁 판형 거리**
      (세 칸 상대오차 합, short+long)를 loop_log 에 `px_err` 로 적고 판정문에 붙였다.
  71회차 실행으로 확인: 판정문이 `조회로는 판정 안 함(공개 0편…) · 경쟁 판형 거리 0.6509 (이번 회차부터 비교한다)`
  로 나왔다. short 0.2653 + long 0.3856 = 0.6509. 다음 회차부터 이 숫자가 줄었는지 늘었는지 자동으로 찍힌다.
  **줄었다고는 아직 말할 수 없다** — 비교 상대가 이번 회차가 처음이다.
- **제목 규칙 재측정**: 쇼츠와 롱폼을 갈라 재니 전체 표의 큰 수치(경고 +560% 등)가 사실상 롱폼 수치임을 확인했다.
  쇼츠 80편에서는 어느 패턴도 2배 차이를 못 만든다(따옴표 +32%가 최대, 물음표 +0%).
  planner.py 의 title_patterns() 도 '조회 가중 비율'에서 '조회 중앙 차이'로 자를 통일했다.
  자를 바꾸자 같은 말뭉치에서 숫자 -2% · 이유·방법 -10% 로 나왔다 — 예전 자로는 숫자가 48%로 찍혀 중요해 보였다.
  자세한 표는 work/research/series-plan.md 맨 위.
- **숏폼 1편 생산·업로드(비공개)**: "서울 아파트 387쌍, 20% 넘게 내린 38쌍과 중앙값 +2.2%"
  https://youtu.be/rE1y4P6hyUk · 장면 5개 전부 chartimg 막대 차트 · 대본은 내가 직접 썼고(제미나이 안 씀)
  shortscript check 통과 · 소재는 오늘 발행한 peakdrop 글의 사실표(국토교통부 실거래가 직접 집계).
  제목은 이번 회차에 갱신한 쇼츠 규칙(28~40자, 숫자, 경고어·물음표 억지로 안 넣음)을 따랐다.
- **화면 검증에서 찾은 chartimg 결함 3가지**(식 검증으로는 안 잡혔다. 전부 고치고 다시 그려 눈으로 확인):
  (1) *음수 값 글자가 안 보였다*. 음수 막대는 값을 막대 안쪽에 **배경색**으로 적는데, 값 글자가 막대보다
      길면 막대를 넘어 검은 바탕 위 검은 글씨가 됐다. 구별 하락률 차트에서 강북 -2.8% 가 통째로,
      도봉 -11.9% 는 '%'가 사라졌다. 이제 길이를 재서 안 들어가면 0선 오른쪽 빈 자리에 막대 색으로 적는다.
  (2) *가장 긴 음수 막대가 이름을 덮었다*. 0선은 0.45 자리인데 막대 길이 상한은 0.52라 x0 를 넘었다 → 0.42로.
  (3) *표시용 글꼴에 없는 글자가 조용히 사라졌다*. BlackHanSans 에 '㎡'·'²'가 없어
      '전용 53.16㎡ 실거래' 가 '전용 53.16  실거래' 로 나왔다(자리도 안 차지한다). ㎡→m2 등으로 바꿔 그린다.
  (4) 덤: 줄이 둘뿐인 차트는 막대 둘이 화면 위아래 끝에 따로 떨어져 보였다 — 줄 간격에 상한을 두고 가운데로 모았다.


## 2026-09-24 (13시 A회차) — 색인율이 낮았던 게 아니라 재는 방법이 틀렸다

오늘 perf.py를 돌리니 발행 12편이 **전부 색인 안 됨·전부 30위 밖**으로 나왔다. 어제·그제 기록도 색인 67%였다.
계기판의 `색인율 0.167`도 여기서 나온 값이다. 그런데 재는 방법을 뜯어 보니 숫자 셋이 전부 측정 탓이었다.

**1) 모든 글을 '발행 당일'에만 쟀다.** perf.py는 RSS 최근 N편을 잰다. 우리는 하루 12~15편을 올리므로
어제 글은 오늘이면 이미 최근 12편 밖이다. 그래서 지금까지 **한 글도 색인될 시간을 준 적이 없다**.
날짜별 기록을 세어 보면 그날 발행분은 9/24 0/12(0%)인데 하루 지난 기록은 9/22·9/23 모두 8/12(67%)다.
→ perf.py에 '지난 글 다시 재기'를 넣었다(지난 기록 중 아직 색인 안 된 글을 오래된 것부터 하루 8편까지).

**2) 원고를 못 찾아 색인 판정을 RSS 요약으로 때우고 있었다.** draft_index()가 옛 `blog.txt`만 읽는데
지금 원고는 `pkg/title.txt` + `pkg/b00~b0N.txt` 조각이다. 실측: blog.txt가 있는 폴더 3개, 조각이 있는 폴더 47개.
고친 뒤 원고가 잡히는 글이 **3편 → 51편**이 됐다. 고치기 전 재측정은 8편 전부 "본문 없어 못 잼"이었다.

**3) 제목 검색 1위인 글을 '색인 안 됨'으로 적고 있었다.** 색인 판정을 본문 문장 따옴표 검색으로만 했는데,
9/23 발행 3편이 제목 검색 1위이면서 본문 문장으로는 안 잡혔다. 검색 결과에 우리 글이 있으면 네이버가 그 글을
아는 것이므로 색인된 것이다. → 제목 검색에서 잡히면 색인으로 본다.

**셋을 고치고 다시 잰 결과: 지난 글 5편 중 4편이 뒤늦게 색인됐다(80%).**
9/23 발행 「코픽스 9월 공시」·「10월 배당 일정」·「근로장려금 2026」은 제목 검색 **1위**다.
아직 안 된 것은 「건강보험료율 2026」 1편(30위 밖). 9/22 [공유] 글 3편은 우리 원고가 아니라 판정 대상이 아니다.

**형식·축별 성과는 아직 셀 수 없다.** 이번에 만든 `work/formperf.py`로 형식(pkg/form.txt)·축(pkg/axis.txt)별
색인율·10위 안 비율·카페 조회 중앙값을 한 번에 세게 했지만, 하루 넘은 글 중 형식 표기가 있는 것이 **블로그 1편**뿐이라
어떤 형식이 좋다고 말할 수 없다(카페도 5편). 표기 없어 뺀 글이 블로그 12편·카페 33편이다.
하루치로 단정하지 않는다 — 오늘 고친 재측정이 매일 8편씩 쌓이면 며칠 뒤부터 형식별 비교가 가능해진다.
만드는 과정에서 카페 조회를 '가장 최근 기록'으로 집계해 전부 0회로 나온 것도 고쳤다(조회는 시간이 지나야 쌓이므로
가장 큰 값을 쓰고, 올린 지 하루가 안 된 글은 뺀다). 축 표기에 `us`·`대출`처럼 표준 4축 밖의 값이 섞여 있다.

**경쟁 발행 빈도(blogfreq, "DSR 한도"·"20대 순자산"·"연금소득세" 상위 30블로그):**
편/일 중앙값 3.92, 상위 10% 17.0, 하루 최다 중앙값 8편, 최대 45편. 편/일 5 이상 11곳·10 이상 5곳.
우리 12~15편/일은 상위 노출권 안에서 많은 축이지만 전례 없는 수준은 아니다(naver-abusing-rules.md의 12.75편/일과 같은 결).

## 2026-09-24 13:45 회차 — 얼어 있던 short 판형 오차의 정체

지난 여러 회차 동안 `short.split_scale` 이 "2.0 유지 (격자 중 가장 작다)"로만 보고되고 판형 거리
0.6509 가 한 번도 움직이지 않았다. 원인을 두 군데에서 찾았다: 격자가 2.0 에서 끝나 최선이 늘 **끝점**에
붙어 있었고(오차 1.0121→0.728→0.5784→0.2653, 계속 내려가는 중이었다), 동시에 thumb.py 가 그릴 때
split_scale 을 2.0 으로 깎고 있었다. 끝점에 붙은 "유지"는 수렴이 아니라 격자가 짧다는 뜻인데 그 구별이
로그에 없었다.

고친 것: (1) thumb.py 의 split_scale 한계 2.0 → 3.2 (진짜 한계는 그 아래 fit() 폭 맞춤이라 판을 넘치지
않는다) (2) loop.py 에 KNOB_LIMIT 과 격자 자동 연장 — 최선이 끝점에 붙으면 한계까지 점을 더 재고,
한계에서도 끝점이면 blocked 에 "그리기 한계에서 멈췄다"로 남긴다.

**재서 나온 답(73회차, 6점 실측)**: 2.4 는 실제로 더 크게 그려졌다(text_bot 0.3008 → 0.3682) —
한계 변경이 먹었다는 증거다. 그런데 경쟁 목표 0.3093 을 **지나쳐** 오차가 0.2653 → 0.3904 로 올랐고,
2.8 은 2.4 와 완전히 같은 수(폭 맞춤 천장)였다. 즉 2.0 이 진짜 최소였다.

**그래서 판형 거리는 줄지 않았다(0.6509 그대로).** 줄었다고 쓰지 않는다. 대신 얻은 것은,
전에는 구별할 수 없던 "수렴"과 "격자가 짧음"을 이제 숫자로 가른다는 점이다. 최선이 안쪽(2.0)에서
나왔으므로 이 손잡이는 더 밀 데가 없다.

**남은 오차의 주인(다음 회차 첫 일감)**: 0.2653 은 이제 text_top 0.2423(목표 0.2748, 부족)과
text_mid 0.4748(목표 0.4241, 과다)이 거의 전부다. text_bot 은 0.3008 vs 0.3093 으로 이미 맞았다.
윗줄만 키우고 가운뎃줄 몫을 줄이는 손잡이가 **없다** — split_scale 은 아랫줄 둘만 키운다.
그래서 다음 회차에 thumb.py 에 윗줄 전용 배율(top_scale, 폭 맞춤 안에서만)을 손잡이로 만들고
loop.py 의 tune_bands 세 번째 손잡이로 붙이는 것이 남은 0.2653 을 줄이는 유일한 길이다.

## 2026-09-24 14:10 성과·생산 기록

- 블로그 방문: 오늘 47명, 누적 11,164명(전체 어제보다 +57). 13:52 측정 46명에서 한 명 늘었다.
  하루치라 오른 것/내린 것을 단정하지 않는다.
- 영상: 채널 총조회 3,527회로 지난 회차와 같다(+0). 구독 39명. **최근 10편이 전부 private** 이라
  조회가 0에 묶여 있고, 그래서 이 회차에도 썸네일 조정 효과를 조회로는 판정하지 못했다(px_err 로만 잼).
- 이번 회차 숏폼: **1년 정기예금 39건 금리** https://youtu.be/4YhL6JsSzoQ (private)
  - 1차 출처: 금융감독원 금융상품통합비교공시 API, 공시월 202609, 은행권 12개월. 39건을 직접 받아 계산했다.
  - 대본은 내가 썼다(제미나이 아님). shortscript check 통과 — 처음엔 사실표에 없는 숫자 '2.1'·'27'·'3.6'
    으로 세 번 걸렸고, 2.10%·3.60% 로 표기를 맞추고 27건(39−12)은 사실표에 계산을 적어 넣어 통과시켰다.
  - 장면 5개 모두 자료 화면을 깔았다(chartimg 막대 4장: 금리순위·이자차이·조건차이·조건유무). 글자만 있는 장면 없음.
- 색인(perf.py 12): 오늘 발행 12편은 12편 모두 제목검색 30위 밖·색인 안 됨. 다만 **어제치를 다시 재니
  5편 중 4편(80%)이 뒤늦게 색인됐고 그중 3편이 1위**(코픽스 9월 공시, 10월 배당 일정, 근로장려금 2026),
  9월 21일 글은 3위였다. 즉 "당일 색인 0편"은 실패가 아니라 시차로 보인다 — 오늘치로 단정하지 않고
  다음 회차에 같은 12편을 다시 재서 확인한다. 건강보험료율 2026(9/23)은 아직 안 됐다.
- 이번 회차에 못 한 것: 새 도구 조사(WebSearch 무료 오픈소스 탐색, 하루 1회 항목). 25분 안에
  일감표·루프 차이 고치기·성과·숏폼 생산까지 하고 시간이 남지 않았다. 지어내지 않고 못 했다고 적는다.

## 2026-09-24 15시 회차 (B: 내일 기획 + 자료조사 선행)
- planner.py에 대기 묶음 현황 줄을 붙였다. 편성표 첫 화면에서 "블로그 n개·카페 n개 / 목표 각 3개"가 보인다. 9/23 16·17시 발행 회차가 대기 0으로 시작해 0편으로 끝난 일이 원인이다.
- 안 쓰던 도구 offitable.py 15를 돌렸다. 545곳 계산, 수익률 중앙값 4.77%, 건축물대장 20/20. 상위 5줄을 월세x12 / (매매-보증금)으로 다시 계산해 10.00·9.82·9.13·8.84·8.57% 전부 일치. 주상복합(방화샤르망1)은 지시대로 주차 숫자를 적지 않고 사유를 달았다.

## 2026-09-24 16시 회차 (자가개선 루프 75·76회차)

**루프가 스스로 못 고친 것을 고쳤다 — 원인은 내가 만든 것이었다.**
74회차에 경쟁 판형 거리가 0.2653 → 2.5986 으로 뛰었다. 내가 loop.py 를 배경과 앞에서 겹쳐
돌린 탓이다. 두 바퀴가 같은 design.json 과 같은 샘플 그림을 쓰는데, 앞 바퀴가 그린 그림을 뒤 바퀴가
재는 바람에 text_spread 격자 7점이 전부 같은 값(0.2653)으로 나왔다. 그리고 min() 이 '전부 같음'에서
격자 맨 아래값(0.0)을 골라 손잡이를 끝까지 밀었다 — 세 줄이 가운데로 뭉쳐 text_bot 이 0.3008 → 0.0.
- loop.py: 지금 값과 비기면(1e-4 안쪽) 지금 값을 유지한다. 손잡이가 안 듣거나 측정이 섞여도 안 밀린다.
- loop.py: .loop.lock 으로 한 번에 한 바퀴만 돈다.
- design.json: text_spread 0.45 · split_scale 2.0 으로 되돌렸다.
- 75회차 실측: 경쟁 판형 거리 2.5986 → 0.6542, short 세 칸 오차 2.213 → 0.2686.
  **text_top·text_mid·text_bot 세 항목이 '남은 차이'에서 전부 빠졌다.** 두 격자 다 '유지'로 나온다.
  (9/23 기록에 "top_scale 손잡이를 새로 달아야 남은 0.2653 을 줄인다"고 적어 뒀는데, 그럴 필요가 없어졌다.)

**화면 검증이 숫자 검증을 뒤집었다.** 75회차가 text_tint 를 0.726 → 1.0(한계)로 올렸다. 숫자로는
white 가 목표에 다가가는데, 실제 썸네일을 열어 보니 글자가 흰빛을 잃고 잿빛 파랑으로 죽어 안 읽힌다.
같은 배경·문구로 0.7 / 0.85 / 1.0 을 그려 눈으로 견줬고 0.7 에서만 글자가 희게 남았다.
tint_v 하한 0.75 와 같은 이유로 text_tint 윗한계를 0.7 로 막았다. short.white 는 앞으로 '막힘'으로 남는다.

**보고가 대기 건을 한 번도 이름으로 안 보여 주고 있었다.** 계기판은 "사람 손 필요 항목 4"를 넉 달째
세는데 12:30 보고에는 그 4건이 무엇인지 안 나왔다 — report.py 정규식이 `| 무엇 | 왜 | 절차 | 대기 |`
모양만 잡고 `| 대기 | 무엇 |` 모양(3건)은 놓쳐 "계속 대기 N건" 줄이 아예 안 찍혔다. 고쳐서 실행 확인:
MCP 커넥터 · 유튜브 숏폼 공개 전환 · 브이월드 데이터 API 권한 · 네이버 클립/릴스/틱톡 업로드.
숫자 4는 사장님이 손대야 줄어든다. 이제는 무엇에 손대야 하는지가 보고에 나온다.

**성과**(하루치라 오른 것/내린 것을 단정하지 않는다)
- 블로그 방문 47명(14:10 측정과 같다). 색인: 오늘 12편은 12편 다 30위 밖·색인 안 됨.
  어제치를 다시 재니 5편 중 4편(80%)이 뒤늦게 색인됐고 **1위가 3편**(코픽스 9월 공시, 10월 배당 일정,
  근로장려금 2026), 9/21 글이 3위. 14:10 회차가 "당일 0편은 시차로 보인다"고 적어 둔 것이 이번에도 같다.
  건강보험료율 2026(9/23)은 아직 안 됐다. 9/22 공유글 3편은 본문이 없어 못 잰다.
- 영상: 채널 총조회 3,527회로 그대로(+0). 구독 39명. 최근 10편이 전부 private 이라 이번에도 조회로는
  판정하지 못했다(px_err 로만 잼).

**이번 회차 숏폼**: 9월 30일 배당락 38건 https://youtu.be/DIRJjBL0PjU (private)
- 1차 출처: 나스닥 배당 캘린더 API 2026-09-24 조회(9/24~10/2 99건) · 미국 SEC Investor.gov 배당락일 안내
  · 종가·원달러 Yahoo Finance. 오늘 발행한 블로그 글(224421590827)의 사실표를 그대로 썼다.
- 대본은 내가 썼다(제미나이 아님). shortscript check 한 번에 통과.
- 장면 5개 모두 자료 화면을 깔았다(chartimg 막대 4장: 날짜별 건수·주당 배당금·연 환산 배당률·100주 원화).
  영상 프레임 2장을 실제로 열어 확인했다 — 글자는 위, 차트는 아래로 나뉘어 둘 다 읽힌다.

**이번 회차에 못 한 것**: 새 도구 조사(하루 1회 항목)와 경쟁 제목 재분석. 74회차 되돌리기와 화면 검증에
시간을 썼다. 지어내지 않고 못 했다고 적는다.

## 2026-09-24 18:30 회차 (자가개선 루프)

**이번 회차에 닫은 것 두 가지 — 둘 다 "재는 방식이 틀려서 영영 안 닫히던" 항목이다**
- 글 루프 판정이 **46회 연속 보류**였다. 원인은 textloop `pick_cut` 이 기준 회차를 카페+블로그를 **합친**
  편수로 고르는데 채점(`judge_side`)은 매체별로 따로 한다는 것. 합쳐서 4편이면 카페 2·블로그 2로 갈려
  양쪽 다 보류가 났다. 같은 시각에 19:36 회차를 잡았으면 카페 7·47, 블로그 4·10 으로 둘 다 채점됐다.
  기준 선택을 채점과 같은 잣대로 바꿨다. 확인: 보류 46 → 0회, 계기판 점수 138 → 0.
  첫 판정: 2026-09-23 19:36 기준 · 카페 하루당 조회 1.16 → 1.12 ↓ · 블로그 색인+순위 2.00 → 2.00 =.
- 썸네일 루프의 **미해결 차이 2건**(short.white · short.contrast)은 사실 "못 푼 숙제"가 아니라
  **풀지 않기로 한 것**이었다. 둘 다 화면 검증에서 "더 밀면 글자가 잿빛으로 죽는다"고 한계를 박아 둔 자리다.
  그런데 계기판이 이것을 미해결로 세니 회차마다 끝난 판단을 다시 뒤졌다 — 75회차가 text_tint 를 1.0 까지
  올렸다가 눈으로 보고 되돌린 것이 그 결과다. loop.py 에 CAPPED 표를 넣어 '막힘'과 '한계 확정'을 갈라 적고,
  health.py 는 한계 확정을 **점수 0짜리 별도 줄**로 보이게만 한다. 숨기는 것이 아니라 줄일 것에서 뺀 것이다.
  확인(81회차): "남은 차이: 없음 — 남은 것은 한계 확정 short.contrast, short.white".

**루프 한 바퀴**(79·81회차): 경쟁 썸네일 296장 재측정. text_spread 0.45 · split_scale 2.0 · long.text_y 0.5
모두 격자에서 지금 값이 최소 오차라 유지. 경쟁 판형 거리 0.6543 그대로.

**성과**(하루치라 단정하지 않는다)
- 블로그 방문 오늘 53명(17시 회차 47명에서 +6), 전체 누적 11,170(어제보다 +63).
- 색인: 오늘 12편은 12편 다 30위 밖·색인 안 됨. 어제치를 다시 재니 5편 중 4편(80%)이 뒤늦게 색인됐고
  **1위 3편**(코픽스 9월 공시 / 10월 배당 일정 / 근로장려금 2026), 9/21 글이 3위. 17시 회차와 같은 모양이라
  "당일 0편은 시차"라는 읽기가 두 회차 연속 같다. 건강보험료율 2026(9/23)은 아직 안 됐다.
- 영상: 채널 총조회 3,527로 그대로(+0). 구독 39명. 최근 10편이 전부 private 이라 조회로는 판정하지 않는다.

**이번 회차 숏폼**: ACE 미국배당다우존스 분배금 10회 https://youtu.be/z9dTCmWdSBo (private)
- 1차 출처: 한국투자신탁운용 ACE ETF 공식 상품페이지 분배금 현황표 10건(2026-09-24 조회).
  묶음 work/research/acediv0924/pkg/facts.txt 의 숫자만 썼다.
- 대본은 내가 썼다(제미나이 아님). shortscript check 통과(두 번째 시도 — 첫 판에서 사실표에 없는 '2025'·'14,691',
  줄 26자 초과 셋을 잡아 줬다).
- 장면 5개 모두 chartimg 막대(월별 분배금 10개 · 세전세후 · 필요 원금 · 1,000만원 · ACE vs TIGER 순자산).
  프레임 2장을 실제로 열어 확인했다 — 글자는 위, 차트는 아래로 나뉘어 둘 다 읽힌다.
- **길이 39초**로 규칙(45~60초)보다 짧다. `shortscript.py check` 는 글자 수만 보고 **읽는 시간을 재지 않는다**.
  다음 회차가 볼 것: check 에 say 글자 수 합계로 대략 길이를 계산해 45초 미만이면 잡아내는 줄을 넣는다.
- TTS(edge-tts)가 한 번 DNS로 넘어졌다가 그대로 다시 돌리니 됐다. 도구 문제가 아니라 일시적인 것.

**이번 회차에 못 한 것**: 새 도구 조사(하루 1회 항목)와 경쟁 제목 재분석. 제목 규칙은 오늘 12:15 회차가
쇼츠·롱폼을 갈라 이미 다시 쟀고, 이번 회차는 위 두 항목을 닫는 데 시간을 썼다. 지어내지 않고 못 했다고 적는다.
일감표 3번(어느 회차도 안 부르는 도구 — kwcomp.py)도 손대지 못했다.

## 2026-09-24 20:0x 루프 82회차

**우리 성과.** 블로그 방문 오늘 59 · 전체 11,176(어제보다 +69) — 19:53 계기판의 54에서 다섯 명 늘었다.
유튜브 채널 총조회 3,527로 **그대로**(0.8시간 +0회). 최근 10편이 전부 private이니 당연한 결과고,
조회로 썸네일·제목 조정 효과를 재는 길은 여전히 막혀 있다(사장님만 풀 수 있는 항목).
perf.py 12편: 오늘 발행 12편 전부 제목검색 30위 밖이고 본문 문장으로도 안 잡힌다.
다만 **지난 글 다시 재기에서 5편 중 4편(80%)이 뒤늦게 색인**됐다 —
9/23 코픽스·10월 배당일정·근로장려금 세 편이 1위, 9/21 파이어 계산기가 3위.
당일 색인 0%는 나쁜 신호가 아니라 **네이버 색인이 하루 걸린다는 것**으로 읽는 게 맞다(4일치 누적).
건강보험료율(9/23)은 아직 안 됐다.

**루프 82회차.** 경쟁 썸네일 296장 재측정. short.text_spread·short.split_scale·long.text_y 전부
격자 최선이 현재값이라 유지. 남은 차이 없음 — 남은 둘(short.white·short.contrast)은 화면으로
한계를 확정한 자리다. 경쟁 판형 거리 0.6543 그대로.

**그런데 화면은 못 쓸 물건이었다.** 아래 '숏폼' 참고 — loop.py 가 "차이 없음"을 낸 회차에
썸네일이 자료 차트를 정통으로 덮고 있었다. 자세한 건 fix-needed.md 맨 아래.

**숏폼.** 브로드컴 배당 https://youtu.be/sLO1YW3tf0c (private).
대본은 내가 직접 썼고(Gemini 안 씀) shortscript check 통과. 장면 4개 전부 chartimg 차트를 깔았다.
검사기가 '5.78'·'295.91' 을 사실표에 없는 숫자로 잡아 줘서 '5억 7,826만원'·'295억 9,100만 달러'
원문 표기로 고쳤다 — 이 검사기는 값을 한다.

**일감표.** 2번(어느 회차도 안 부르는 도구 = kwcomp.py)을 닫았다. 1·3·4번은 다른 회차(improve B/C/F) 소관.

## 2026-09-25 01:35 (loop 83~84회차)
영상: 채널 총조회 3527, 지난 회차와 같다(+0). 최근 10편이 전부 private 이라 조회가 0이고,
늘어난 조회가 있어도 옛 공개 영상 몫이라 이번 조정의 효과로 읽을 수 없다. 하루치로 단정하지 않는다.
방문자: 오늘 0 / 전체 11186 (어제보다 +10).

판형 거리는 0.6543 에서 회차가 바뀌어도 안 움직인다(short 0.2687 + long 0.3856).
84회차에 long 쪽을 뜯어봤고, 글자 손잡이로는 못 닫는다는 것을 실측으로 확인했다(loop.py 주석에 표 남김).
정리하면 롱폼 차이는 배경에 자료 화면이 깔려 있느냐의 문제지 글자를 어디 놓느냐가 아니다.

지금 구조의 한계를 적어 둔다: 썸네일 손잡이를 84회차 돌렸지만 그 결과를 조회로 판정한 적이 한 번도 없다.
전부 비공개라 조회가 영영 0이고, 그래서 루프는 '경쟁 판형과의 거리'라는 대리 눈금만 보고 있다.
대리 눈금이 0.6543 에 얼어 있는데 진짜 눈금은 아예 못 읽는 상태다 — 공개 전에는 어느 조정도 검증되지 않는다.

### 덧 (01:45) — 계기판이 왜 22분이나 걸렸나
selfloop 출력이 뒤늦게 들어와서 확인했다. 1345초 중 900초가 '발행 감시' 한 줄에서 버려졌다.
watchdog.py 는 브라우저 프로필을 쓰는데 그 프로필은 한 번에 한 회차만 연다. 발행 회차가 쥐고 있으면
naverpost.acquire_lock 이 900초를 기다리고, selfloop 의 timeout 도 900초라 반드시 ERR 로 끝난다.
잠금은 실제로 살아 있는 PID 802948(01:16 에 같이 뜬 발행 회차)이 쥐고 있었다 — 죽은 잠금이 아니다.
기계가 21:13~01:16 자는 동안 밀린 예약 넷이 01:16 에 한꺼번에 떠서 겹친 것이 방아쇠였다.
고침: 주인이 살아 있으면 건너뛴다(감시는 매시 예약으로 따로 돈다). 건너뛰는 판단은 naverpost._alive 를
그대로 빌려 쓴다 — 두 군데서 다르게 세지 않게.
watchdog_log.json 마지막 성공 기록도 2026-09-24 21:02 로, 맥박이 말한 4.4시간 공백과 맞는다.

## 2026-09-25 02:05 (loop 85~86회차)

**우리 성과.** 영상: 채널 총조회 3527 — 지난 회차와 똑같다(+0). 최근 10편 전부 private 이라 조회가 0이고,
이번 조정의 효과로 읽을 수 없다. 방문자: 오늘 2 / 전체 11188 (어제보다 +12).
블로그 색인: 최근 12편은 12편 다 30위 밖·색인 안 됨(발행 당일 글이라 이르다).
지난 글 5편을 다시 재니 4편(80%)이 뒤늦게 색인됐고 그중 3편이 1위, 1편이 3위다.
색인은 발행 즉시가 아니라 하루 이틀 뒤에 붙는다 — 당일치로 색인 실패를 단정하면 안 된다.

**이 회차가 닫은 차이: 검은 여백을 경쟁사 그림으로 세고 있었다.**
남아 있던 차이 두 개(short.white, short.contrast)는 둘 다 '한계 확정'이었다. 손잡이(text_tint, tint_v)를
더 밀면 숫자는 맞는데 글자가 죽어서 2026-09-24에 세워 둔 것이다. 그래서 손잡이가 아니라 측정을 봤다.

경쟁 '쇼츠' 썸네일 41장이 **전부 가로형(1280x720)** 이었다. 유튜브는 9:16 영상의 썸네일도 16:9 칸에 담고
남는 자리를 새까맣게 채운다. 41장 중 19장이 그랬고, 가운데 실제 그림은 가로의 71%뿐이었다(중앙값).
그 검은 여백까지 면적 비율의 분모로 세는 바람에 기준값이 전부 묽어져 있었다.
우리 쇼츠(1080x1920)는 여백이 없으니 늘 기준보다 높게 나왔다 — 없는 차이를 쫓고 있었다.

고침: thumbstat.measure 가 재기 전에 새까만 여백을 떼어 낸다(debar). 그림을 바꾸는 게 아니라 여백을 안 센다.
롱폼도 255장 중 146장이 같은 여백을 갖고 있었다.

**숫자로 확인(85회차 → 86회차, 같은 그림·측정만 바뀜):**

| 항목 | 85회차(여백 포함) | 86회차(여백 뗀 뒤) |
|---|---|---|
| short.contrast | 우리 0.2294 vs 경쟁 0.1791 (차이 28%) | **차이 항목에서 사라짐** (경쟁 0.2085, 차이 10% < 25% 문턱) |
| short.white | 우리 0.0378 vs 경쟁 0.0107 | 우리 0.0412 vs 경쟁 0.0158 (차이 0.0271 → 0.0254) |
| 쇼츠 판형 거리 | 0.2687 (다섯 회차째 그대로) | **0.1912** |

판형 거리 0.2687 은 82~85회차 내내 1의 자리도 안 움직였다. 여백을 떼자 격자 실측의 최선값이 달라져
text_spread 0.45 → 1.0, split_scale 2.0 → 1.6 으로 처음 움직였고 거리가 0.1912 로 내려갔다.
가려져 있던 차이 셋(short.yellow, long.text_mid, long.dark)도 드러나 그중 둘을 손잡이로 닫았다
(yellow_tint 0.4497 → 0.4956, long.bg_bright 1.7447 → 1.2898).

**단서 하나.** 롱폼 판형 거리 0.3856 → 0.5238 은 나빠진 게 아니라 **견줄 수 없는 수**다.
롱폼도 255장 중 146장의 기준값이 같이 바뀌었으니 자가 달라진 것이다. 86회차의 0.5238 이 새 자의 출발점이고,
롱폼이 실제로 줄고 있는지는 87회차부터 견주면 된다. 여기서 "롱폼이 나빠졌다"고 적으면 거짓말이다.

## 2026-09-25 04:06 (loop 88~89회차) — 롱폼 차이를 '못 닫는 항목'으로 확정. 썸네일이 좋아진 건 아니다

**먼저 분명히 해 둘 것: 이번 회차에 썸네일이 나아지지 않았다.** 경쟁 판형 거리는 롱폼 0.5584·쇼츠 0.1912 로
지난 회차와 똑같다(0.7496 → 0.7496 그대로). 바뀐 것은 그림이 아니라 **분류**다.

82~87회차 내내 `long.text_mid` 가 '미해결 차이'로 잡혔고, 회차마다 같은 격자(text_y 0.15~0.80)를 처음부터
다시 훑고는 같은 자리에서 멈췄다. 이번에 끝까지 재서 **그리기로는 닿을 수 없는 숫자**임을 확정했다.

| 무엇을 의심했나 | 무엇을 쟀나 | 결과 |
|---|---|---|
| 측정이 우리 그림에 안 맞나 | 팽창 커널 25x9x2 → 13x5x1 → 9x3x1 → 5x3x1 | **측정 탓 아님.** 경쟁 상·하위는 더 잘 갈렸지만(mid 0.943/0.890 → 0.636/0.452) 우리와의 거리는 되레 벌어졌다(우리 롱 mid 0.624 → 0.193, 상대 -34% → -70%) |
| 패널이 배경 가장자리를 지우나 | panel_alpha 212/160/110/60/0 | mid 0.450~0.490, **bot 0.363 고정** |
| 배경이 너무 어두운가 | bg_bright 0.5/0.9/1.29/1.6/1.8 | mid 0.270~0.453, **bot 0.363 고정** |
| 쇼츠처럼 3줄로 나누면 되나 | lines=3, split 1.0/1.6/2.2 | 오차합 1.4469/1.3301/1.1284 — 2줄(1.1829)보다 **안 나아지고** bot 이 0.363 → 0.190 으로 되레 비었다. 되돌렸다 |

원인은 하나 남는다. **경쟁 롱폼 기준값 자체가 '글자 배치'가 아니다.** 255장 중앙값이
text 0.9433 · text_top 0.9211 · text_mid 0.9429 · text_bot 0.9714 · **text_thick 1.0** 이다 —
글자 지도가 화면을 통째로 덮은 값이고, 경쟁 롱폼이 사진·얼굴·그래픽으로 프레임을 꽉 채워서 나온 수다.
우리 롱폼 배경은 차트라 아래 1/3이 비어 있고, 어떤 손잡이로도 그 칸은 안 채워진다.

조치: loop.py 에 `BAND_CAPPED` 를 두어 `long.text_mid` 를 '닫히지 않는 항목'으로 갈라 세웠다.
89회차 출력이 **"남은 차이: 없음 — 남은 것은 한계 확정 long.text_mid, short.white"** 로 바뀌었다.
둘 다 이유가 적혀 있는 항목이다(short.white 는 2026-09-24 화면 검증). 배경을 바꿀지는 가독성과 맞바꾸는
판단이라 tools-wanted.md 에 사람 몫으로 올렸다.

**성과 숫자(지난 회차 대비)**
- 유튜브: 채널 총조회 3527 → 3527(+0), 구독 39, 영상 6편. **최근 10편이 전부 private 이라 조회로는 아무것도 판정 못 한다.**
  썸네일·제목을 아무리 고쳐도 공개가 0편이면 효과를 영영 못 잰다 — 계기판에 사장님 몫으로 올라가 있다.
- 블로그 방문: 09-23 전체 11107 → 09-24 11176(+69) → 09-25 04:04 11194(+18, 오늘 8명).
  오늘치는 새벽 4시 수치라 하루치로 견주면 안 된다. 다만 02:29부터 네이버 등록 장애로 오늘 발행이 0편이라
  (tools-wanted.md) 오늘 하루는 방문도 눌릴 것으로 본다. **하루치로 단정하지 않는다.**

**이번 회차에 만든 숏폼** — https://youtu.be/1BIsAmCNOxk (private)
「40대 순자산 평균 4억 8,389만원, 중앙값은 2억 8,384만원」 · 장면 5개 전부 막대 차트를 깔았다.
대본은 내가 직접 썼고(제미나이 안 씀) shortscript check 를 통과했다. 이걸로 최근 11편 중 공개 0편이 된다 —
**공개 전에는 이 영상도 조회가 영영 0이라 썸네일·제목 조정 효과를 잴 수 없다.** 계기판의 사장님 몫 항목이다.

---

## 2026-09-25 06:0x · loop 90·91회차 (자가개선 회차)

**우리 성과(지난 회차 04:04 대비).**
- 블로그 방문: 05:58 기준 오늘 9 · 전체 11,195(어제보다 +19). 04:04 에는 오늘 8 · 전체 11,194 였다 →
  **1.9시간에 +1명.** 새벽 2~6시는 원래 사람이 없는 시간대라 이 숫자로 오르내림을 판정하지 않는다.
- 유튜브: 채널 총조회 3,527 그대로(+0). 최근 10편이 **전부 private** 이라 조회가 영영 0이다.
  썸네일·제목 조정 효과는 공개 전에는 잴 수 없다 — 계기판에서 사장님 몫 항목으로 잡혀 있다.
- 블로그 색인(perf.py 12): 어제(09-24) 발행 12편은 **12편 모두 아직 색인 전**이다.
  그때는 이게 실패로 보였지만, **지난 글 다시 재기에서 5편 중 4편(80%)이 뒤늦게 색인됐다** —
  코픽스·10월 배당 일정·근로장려금 세 편은 지금 **제목 검색 1위**, 파이어 계산 글은 3위다.
  **하루 만에 색인 여부로 글을 판정하지 않는다**(적어도 이틀은 둔다).

**경쟁 제목 재분석(design/*/meta.json 459편 다시 셈).**
쇼츠 80편 조회 중앙 1,700 · 롱폼 379편 6,174. 패턴 비율 — 쇼츠: 숫자 43% · 따옴표 25% · 경고어 6% ·
물음표 17% · 길이 중앙 33자 / 롱폼: 숫자 47% · 따옴표 16% · 경고어 7% · 물음표 40% · 길이 중앙 38자.
**series-plan.md 에 적힌 지난 값(따옴표 25%·경고 8%·숫자 43%)과 사실상 같다 → 제목 규칙은 안 바꿨다.**

**썸네일 루프(이 회차의 본업).**
90회차 남은 차이: 세 칸이 top 0.2504(목표 0.2748, -9%) · mid 0.4771(0.4241, +12%) · bot 0.3139(0.3093, 맞음).
즉 남은 오차 0.1912 는 전부 "윗칸이 비고 가운뎃칸이 넘친다" 하나다. 루프가 낸 막힘은
`short.text_spread 가 한계 1.0 에서 멈췄다 — 손잡이를 더 넓혀야 한다` 였는데, **재 보니 그 진단이 틀렸다.**

- **text_spread 를 넓혀도 소용없다(기하로 확인).** 지금 값에서 세 줄의 y는 [161, 851, 1574]이고 칸 경계는
  640/1280 이다. 세 줄이 이미 각자 제 칸 안에 통째로 들어가 있어 더 벌려도 칸 사이로 넘어갈 글자가 없다.
  게다가 1.17 을 넘으면 위·아래 여백 제한(0.04H / H-0.05H)이 걸려 값이 저절로 멈춘다.
- **그래서 윗줄만 키우는 새 손잡이 top_scale 을 달고 loop.py 를 다시 돌렸다 → 효과 0.** 격자 6점
  (1.0·1.2·1.4·1.7·2.0·2.4)이 세 칸 값까지 소수 넷째 자리까지 전부 같았다(0.2504/0.4771/0.3139, 오차 0.1912).
  **이유까지 확인했다**: 윗줄 크기는 `sz = min(세 줄 fit)` 로 정해지는데 루프 샘플 제목은 윗줄이 가장 긴 줄이라
  그 min 을 만드는 것이 윗줄 자신이다(fit 실측: `금천 61% 강남 36%` 115 · `서울 전세가율` 183 · `전수 조사` 260).
  이미 폭을 꽉 채운 줄이라 어떤 배수를 곱해도 fit 이 도로 깎는다.
- **규칙대로 되돌렸다**(thumb.py·loop.py·design.json). 되돌린 자리에 위 이유를 주석으로 남겨 뒀다 —
  다시 달지 않게 하려는 것이다. 91회차 오차는 90회차와 같은 0.1912 로, **이 회차에 판형은 나아지지 않았다.**
- **다음 회차가 잡을 것**: text_top 은 글자 크기로는 못 올린다(폭이 이미 꽉 찼다). 윗줄도 둘로 나눠
  네 줄로 칸을 채우는 쪽을 재 봐야 한다. 그리고 루프 샘플 제목이 한 개라 윗줄 여유가 있는 제목
  (실측 여유 `40대 순자산` 1.23배 · `코카콜라 배당` 1.11배 · `국민연금 수령액` 1.00배)을 못 재는 것도 같이 봐야 한다.

**발행(일감표 1번).** 오늘 0편 회차 2건의 카페 쪽 원인을 찾아 고쳤다 — 아래 커밋 참고.
블로그 쪽 원인은 네이버 서버 오류 화면(`페이지를 찾을 수 없습니다 … 잠시 후 다시 시도해주세요`,
_shots/blog_after_publish.png)이라 우리 코드로 고칠 것이 없다. RSS 되짚기는 이미 들어가 있다.

**못 한 것.** 새 도구 조사(5번)와 숏폼 한 편 생산(6번)은 이 회차에 **안 했다** — 2번(루프가 못 닫는 차이)에
loop.py 두 바퀴(146초 + 195초)와 기하 확인이 들어가 시간을 다 썼다. 지어내지 않고 그대로 적는다.


## 2026-09-25 07:52 loop 94회차 — text_spread 한계를 넓혀 '막힘'을 '재서 고른 값'으로 바꿨다

**2번(루프가 못 닫는 차이).** 92회차가 남긴 막힘은 하나였다 —
`short.text_spread 가 그리기 한계 1.0 에서 멈췄다 — 오차 0.1912 는 손잡이를 더 넓혀야 줄어든다`.
72회차 split_scale(2.0 → 3.2)과 같은 모양이라 같은 방법으로 풀었다.

- **먼저 그리기 한계를 실제로 쟀다**(넓히기 전에). 루프 샘플(1080x1920, 3줄, split_scale 1.6,
  줄높이 135/215/217, cy 963)로 `ys` 를 손으로 계산했다: spread 1.0 에서 윗줄 y=222·아랫줄 y=1570 이라
  위 클램프(76)에도 아래 클램프(1607)에도 **아직 안 닿는다**. 진짜로 붙는 지점은 아랫줄 1.07·윗줄 1.39 다.
  그래서 한계를 1.0 → **1.4** 로 넓혔다(thumb.py·loop.py KNOB_LIMIT 둘 다).
- **넓힌 뒤 격자 9점을 실제로 그려 쟀다. 오차는 안 줄었다** — 0.1912 그대로다:
  `0.6 err 0.2789 · 0.8 0.2801 · **1.0 0.1912** · 1.2 0.3412 · 1.4 0.4096`.
  1.0 을 넘기면 아랫줄은 이미 바닥 클램프에 붙어 안 내려가는데 윗줄만 계속 올라가서
  bot 이 0.3139 → 0.2711 로 목표 0.3093 에서 **되레 멀어진다**(top 도 0.2504 → 0.2221 로 멀어진다).
- **그래도 한계는 되돌리지 않았다.** 되돌리면 '그리기 한계에서 멈췄다'는 막힘이 회차마다 다시 뜨는데
  그건 이제 사실이 아니다 — 1.0 은 격자가 짧아서가 아니라 **재서 고른 최선**이다. 그린 값은 1.0 그대로라
  썸네일은 한 픽셀도 안 바뀐다. 이 회차에 판형 오차는 **나아지지 않았다**(short 0.1912 · long 0.5584, 92→94 동일).
  얻은 것은 오차가 아니라 판정이다: `blocked` 가 1건 → **0건**이 됐다.

**측정 기록 자체를 고쳤다(loop.py `tune_bands`).** 두 번째 손잡이 격자표가 첫 번째 것을 통째로 덮어써서
`loop_log.json` 의 `bands` 에 split_scale 표만 남고 **text_spread 표는 92회차까지 한 번도 안 남았다**.
92회차 막힘을 다음 회차가 로그만 보고는 못 따진 이유가 이것이다. 손잡이별로 따로 남기게 고쳤고
(`bands[kind]` 가 리스트 → `{손잡이: 표}` 딕셔너리), 94회차 로그에 text_spread 9점·split_scale 6점·
long text_y 7점이 **전부** 남은 것을 확인했다. 다른 읽는 곳은 없어서 깨진 데 없다(grep 확인).

**4번 성과.** 블로그 방문 오늘 14·전체 11,200(어제보다 +24). 채널 총조회 3,527 → 3,535.
`perf.py 12`: 어제(9/24) 글 12편은 **12편 다 제목검색 30위 밖·색인 안 됨**. 다시 잰 지난 글 5편 중
**4편(80%)이 뒤늦게 색인**됐고 그중 3편이 1위, 1편이 3위다(코픽스·10월 배당 일정·근로장려금·파이어 계산기).
당일 0% → 하루 뒤 80% 라는 지금까지의 모양 그대로다. 하루치로 단정하지 않는다.

**썸네일 튜닝을 조회로 판정할 길이 아직 없다.** `ytupload.py stats`: 영상 10편이 **전부 private·조회 0**,
채널 공개 영상은 옛 6편뿐이다. 94회차째 픽셀 거리로만 맞추고 있고 클릭률 되먹임은 **한 번도 없었다**.
계기판의 '공개한 영상 수 0/10' 이 이 뜻이다 — 사장님만 풀 수 있다.

**못 한 것.** 3번(제목 재분석)·5번(새 도구 조사)·6번(숏폼 생산)은 이 회차에 **안 했다**.
2번에 loop.py 세 바퀴(182+176+142초)와 기하 계산·로그 구조 수정이 들어가 시간을 다 썼다. 지어내지 않고 그대로 적는다.


---

## 2026-09-25 09:53~11:2x · 자가개선 루프 95~97회차 (개선기)

### 1번 루프 한 바퀴 — "남은 차이: 없음"
95회차 345초. `short.text_spread 1.0`·`split_scale 1.6`·`long.text_y 0.2` 전부 **유지**,
바뀐 손잡이 0개. 남은 두 항목은 이미 한계 확정이다(`long.text_mid` 우리 0.6294 vs 경쟁 0.9429,
`short.white` 0.0412 vs 0.0158). 경쟁 판형 거리 **0.7496 → 0.7496**, 89회차부터 **여섯 바퀴째 그대로**.

### 2번 — 루프가 안 발전한 진짜 이유를 찾았다: **새 재료가 한 번도 안 들어왔다**
2번은 "루프가 못 닫는 차이를 내가 닫는다"인데, 닫을 차이가 없었다(둘 다 한계 확정).
그래서 왜 여섯 바퀴가 같은 답만 내는지를 팠고, 수집 문에서 나왔다.

`loop.py` 1)수집 문이 마지막 **수집** 시각이 아니라 마지막 **회차** 시각(`log[-1]['at']`)과 견주고 있었다.
이 루프는 두 시간마다 도니까 그 간격은 늘 2시간 안쪽 — **12시간을 영영 못 넘는다.**

실측(`loop_log.json` 97회차 전수):
- `did` 에 '경쟁 수집'이 뜬 회차 = **딱 한 번**, 2026-09-23 18:02(첫 바퀴).
- `design/*/meta.json` 12개 최종 수정 = **2026-09-23 18:02** 그대로. 영상 제목 459편 고정.
- 지금까지 **40.3시간** 동안 경쟁 표본이 한 장도 안 늘었다.

즉 89~97회차가 **같은 296장·같은 459개 제목**에서 같은 답을 아홉 번 다시 뽑고 있었다.
루프가 수렴한 게 아니라 **입력이 얼어 있었다**. 고친 것:
- 수집 시각을 `design.json['collected_at']` 에 따로 적고 그것과 견준다. 지금 값은 로그에서 확인한
  실제 마지막 수집 시각(2026-09-23 18:02)으로 넣었고, **고친 문이 열리는 것을 확인**했다(40.3시간 > 12시간).
  다음 회차가 실제로 수집한다.
- 수집 상한 2400초(40분)를 **1200초**로 줄였다. 회차 예산(loop 40분)과 같은 상한이라 수집이 길어지면
  바퀴가 통째로 예산을 넘기고, 그게 9/24 감시기 회차가 16시간 안 끝난 것과 같은 모양이다.
- `sh()` 가 시간초과에 죽지 않고 **받은 출력까지 돌려주게** 했다. 전에는 `TimeoutExpired` 가 그대로 올라와
  바퀴 전체가 죽었다 — 상한이 곧 바퀴를 죽이는 장치였다. 수집은 다음 회차에 이어받으면 되는 일이다.

### 2-b 같은 자리에서 하나 더 — 안 바뀐 296장을 매 바퀴 다시 쟀다
`thumbstat` 은 순수 계산이라 같은 파일이면 같은 `spec.json` 이 나온다. 그런데 매 바퀴 다시 쟀다.
파일 목록·크기·수정시각 + `thumbstat.py` 수정시각으로 지문을 떠서, 하나도 안 바뀌었으면 건너뛴다.

**실측 검증**(같은 기계, 연속 두 바퀴):

| | 96회차(지문 없음 → 측정) | 97회차(지문 같음 → 건너뜀) |
|---|---|---|
| 한 바퀴 | 333.1초 | **310.9초** |
| 측정 단계 | 경쟁 썸네일 296장 재측정 | 296장 그대로 — 지난 측정값을 쓴다 |

−22.2초. 따로 잰 `thumbstat` 단독 시간 **23.0초**와 맞는다. `spec.json` 은 **바이트까지 동일**했고
격자 결과(text_spread 1.0 / split_scale 1.6 / text_y 0.2)와 판형 거리 0.7496 도 전부 같다.
작은 몫(6.7%)이라 부풀리지 않는다 — 한 바퀴 310초 중 나머지는 격자 재기가 쓴다.

### 3번 제목 재분석 — **다시 셀 것이 없었다**
`design/*/meta.json` 12개가 2026-09-23 18:02 이후 그대로다(영상 459편). 직전 제목 규칙 재측정이
2026-09-24 12:15(71회차)이니 **같은 459개 제목**을 다시 세는 일이고 값이 달라질 수 없다.
그래서 `series-plan.md`·`planner.py` 는 **안 고쳤다**. 위 수집 문이 풀렸으니 다음 회차부터 표본이 늘고,
그때 다시 센다. (`selfloop` 이 돌린 네이버 쪽 제목 분석은 따로 돌았다 — 코픽스 blog 탭 상위 30개.)

### 4번 성과
- 블로그 방문 **오늘 19 · 전체 11,205**(전체 어제보다 +29). 직전 회차 기록은 오늘 14 였다.
- 채널 총조회 **3,535** (95~97회차 내내 그대로). 영상 10편 **전부 private·조회 0** — 썸네일 조정을
  클릭률로 판정할 길이 아직 없다. 여섯 바퀴째 픽셀 거리로만 맞추고 있다.
- `perf.py 12`: 어제(9/24) 글 **12편 전부** 제목검색 30위 밖·색인 안 됨.
  다시 잰 지난 글은 5편 중 **4편(80%)이 뒤늦게 색인**, 그중 **3편이 1위**(코픽스·10월 배당 일정·근로장려금),
  1편이 3위(파이어 계산기). 당일 0% → 하루 뒤 80% 라는 모양이 또 그대로다. 하루치로 단정하지 않는다.

### 4-b 계기판 눈금을 하나 고쳤다 — 제목 순위를 **당일 글**로 재고 있었다
`health.py` 는 색인율은 이미 '하루 지난 글'로 고쳐 놨는데(9/24), **제목검색 10위 안 비율만 당일 글**에
남아 있었다. 당일 글은 아직 색인 전이라 `self_rank` 가 거의 다 `None` — 이 눈금은 구조적으로 0.0 에
붙박이고, 중요도 2를 달고 일감표에 점수 1.2로 영영 남는다. 같은 파일 94행 주석이 색인율에 대해
이미 같은 말을 하고 있었다.

`recheck`(하루 지난 글) 기준으로 바꾸고 **실측 확인**: `0.0 / 목표 0.6` → **`0.8 / 목표 0.6`**, 점수 1.2 → **0.0**.
제목이 나빴던 게 아니라 **자가 틀렸다**. 색인만 되면 우리 제목은 1위로 잡힌다.
하루 지난 글이 없는 날에는 당일 값을 중요도 1로 낮춰 참고용으로만 쓴다.

### 0-a 일감표 맨 위 — **막힘**
[발행] 0편 회차 수 6/0. 원인은 네이버 로그인 만료(401 IP check failure)로 **12시간째**이고,
대기 묶음은 블로그 5·카페 4로 충분하다. 발행 계정 문제라 개선기가 코드로 못 닫는다.
`tools-wanted.md` 에 '사람이 봐야 함'으로 이미 올라가 있다(9/25 08시 회차).
**`py -3.12 work/naverpost.py login` 한 번이면 풀린다.**

### 못 한 것
- **5번 새 도구 조사 · 6번 숏폼 생산: 안 했다.** 2번에 loop.py 세 바퀴(345+333+311초 = 16분)와
  수집 문·시간초과·지문·계기판 수정이 들어가 시간을 다 썼다. 지어내지 않고 그대로 적는다.
- 6번은 어차피 올려도 private 라 조회가 0이다(위 4번). 다음 회차 몫으로 남긴다.

## 2026-09-25 12:10 (loop 99회차)

**재는 자를 고쳤다 — 지금까지 썸네일 목표의 절반은 없는 목표였다.**
`thumbstat.text_map` 은 Canny 가장자리를 25x9 로 두 번 팽창한 것이었다. 그건 "글자"가 아니라
"가장자리가 있나"를 재는 자다. 경쟁 60장 중앙값 실측: 위 0.767 / 가운데 0.922 / 아래 0.903.
그림의 90%가 글자일 리 없다 — 자가 포화돼 있었다. 루프는 그 0.9434 를 롱폼 text_mid 목표로 잡고
쫓다가 못 닿아 98회차에 "한계 확정"으로 세워 뒀다. 닿을 수 없는 게 당연했다.
새 자(글자꼴 성분만: top-hat/black-hat + Otsu + 높이·가로세로비·채움비 거르기)로 같은 60장:
위 0.115 / 가운데 0.081 / 아래 0.010.

**우리 그림을 잘라서 재고 있었다.** `debar`(유튜브 레터박스 깎기)가 우리 쇼츠 short0.png 를
1920 → 1699 로 잘랐다(세로 13%). 위아래가 어두운 우리 판형을 레터박스로 오인한 것이다.
띠를 잘린 그림에서 나누면 글자가 실제보다 위에 있는 것으로 재어진다. 안 깎고 재면 쇼츠 가운데 띠가
0.237 → 0.000 으로 바뀐다. `measure(path, pad=False)` 를 추가해 우리 샘플에는 안 깎게 했다.

**효과(숫자):** 98회차 "남은 차이: 없음 — 남은 것은 한계 확정 2건" → 99회차 "남은 차이 5건"
(long.text_mid·short.text_mid·short.yellow·short.text_top·long.text_top). 전부 기존 손잡이로 움직이는
자리다. 손잡이가 실제로 듣는지도 실측으로 확인됐다 — long.text_y 격자 6점에서 띠 오차 합
134.03 → 79.64 (-41%). 옛 자에서는 같은 격자 7점이 전부 0.56~0.93 사이에 뭉쳐 "유지"만 나왔다.
*이 숫자는 옛 값과 크기를 비교하면 안 된다(자가 바뀌었다). "경쟁 판형 거리 0.82 → 80.8" 도
나빠진 게 아니라 눈금이 바뀐 것이다. 다음 회차부터가 새 눈금의 첫 비교 기준이다.*
100회차가 할 일: 새 자로 long.text_mid 0.5784 가 실제로 목표 0.0784 쪽으로 줄었는지 본다.

**블로그 색인은 실패가 아니라 늦는 것이다.** perf 12: 어제(9/24) 12편은 오늘 전부 색인 안 됨.
그런데 지난 글 다시 재기에서 5편 중 4편(80%)이 뒤늦게 색인됐고, 그중 3편이 1위
(코픽스 9월 공시 / 10월 배당 일정 / 근로장려금 2026), 1편이 3위(파이어 계산기)다.
계기판의 "색인율(당일 글) 0.0" 을 실패로 읽으면 안 된다 — 당일 색인은 원래 거의 0이고
하루 이틀 뒤에 붙는다. 하루치로 단정하지 않는다.

**방문자** 오늘 23 / 누적 11,209 (어제보다 +33). 09-25 11:53 측정 19 → 12:08 측정 23.
13시간 발행 빵꾸(9/24 21:09~9/25 11:30) 뒤 첫 반나절이라 아직 판정할 수 없다.

**영상은 여전히 전부 private.** 최근 10편 공개 0편, 채널 총조회 3,535 로 두 회차 연속 그대로.
비공개인 한 조회가 영영 0이라 썸네일·제목 조정 효과를 잴 길이 없다. 사장님만 풀 수 있다.

**숏폼 한 편 생산(99회차).** 소재는 work/research/o0925/pkg/facts.txt(리얼티인컴 9/8 136번째 배당 인상).
대본은 내가 직접 썼고(제미나이 안 씀) `shortscript.py check` 통과. 장면 4개 전부 chartimg.py 막대차트를
깔았다 — 글자만 있는 장면 없음. 올림: https://youtu.be/k4oleiiaJso (private)
제목 "리얼티인컴 세후 월 100만원, 필요한 주식 3,174주 #Shorts" — titlerule 기준 숫자 2개(권장 0~1개)라
다음 회차가 숫자 1개로 줄여 볼 값이다. 쉼표·명사 끝은 맞췄다.


## 2026-09-25 13시 회차 (임무 A — 성과 재기)

**형식·축을 처음으로 조회수와 붙여 세었다.** 카페 최근 30편 중 29편이 묶음(pkg)과
제목이 맞아, form.txt·axis.txt를 조회수에 붙일 수 있었다. 결과는 이렇다.

| 형식 | 편수 | 조회 중앙값 | 최고 |
|---|---|---|---|
| ③ 일정 | 2 | 1.5 | 2 |
| ① 비교·원문정리 | 10 | 1.0 | 6 |
| ② 계산 사례 | 3 | 1.0 | 2 |
| ④ 기록 | 14 | 0.5 | 14 |

| 축 | 편수 | 조회 중앙값 | 최고 |
|---|---|---|---|
| 세금·연금 | 4 | 1.5 | 6 |
| 배당·현금흐름 | 18 | 1.0 | 9 |
| 부동산 | 3 | 1.0 | 14 |
| 종목 | 4 | 0.5 | 1 |

전체 중앙값 1.0 · 최고 14 · 합계 66회.

**하루치로 단정하지 않는다.** 표본이 작다(형식 ③은 2편, 부동산 축은 3편).
중앙값이 0.5~1.5 사이에 다 몰려 있어서 형식 간 차이라고 부를 수 없는 수준이다.
다만 편수가 가장 많은 ④ 기록(14편)이 중앙값은 가장 낮으면서 최고값(14)은 가장 높다 —
기록 형식은 터지거나 안 터지거나로 갈린다는 가설이 세워진다. 다음 회차가 같은 표를
다시 만들어 이 가설이 유지되는지 본다. 세 번 같은 방향이 나오기 전엔 규칙으로 옮기지 않는다.

**축은 배당·현금흐름에 18/29편이 몰려 있다.** 조회 중앙값은 세금·연금(1.5)이 더 높은데
편수가 4편뿐이다. 축을 바꿀 근거로는 부족하다. 세금·연금 축 편수를 늘려 표본을 만드는 게 먼저다.

**블로그 색인은 오늘도 당일 0이다.** perf 12: 9/24~9/25 발행 12편 전부 색인 안 됨,
제목 검색 10위 밖 12/12. 지난 글 다시 재기에서는 5편 중 4편(80%)이 뒤늦게 색인됐고
그중 3편이 1위, 1편이 3위였다. 어제 적은 "색인은 실패가 아니라 늦는 것"이 이틀 연속 같은 모양이다.

**방문자** 14:04 측정 오늘 26 / 누적 11,212(어제보다 +36). 12:08 측정 23 → 14:04 측정 26.

**경쟁 블로그 빈도**(오늘 만든 묶음의 대표 검색어 3개 — 시총 상위 낙폭 / 맥도날드 배당 / 배당락 종목):
상위 30개 블로그의 편/일 중앙값 3.19, 상위 10%가 12.75편/일, 하루 최다 중앙값 7편, 최대 43편.
편/일 2 이상이 18곳, 5 이상 12곳, 10 이상 6곳. 하루 24편 편성이 이 판에서 과한 양은 아니다.
naver-abusing-rules.md에 적힌 "양이 아니라 패턴"과 어긋나지 않는다.


## 2026-09-25 14시 개선기 회차 — 롱폼 썸네일 목적함수가 엉뚱한 칸을 보고 있었다

**고친 것 ①(일감표 1번) — '0편 회차 수'를 재는 법.**
오늘 0편 회차 6번의 사유를 runs_today.json 의 note 로 전부 읽었다. 여섯 번이 전부 같은 하나였다:
9/24 21:09 이후 이어진 네이버 글쓰기 IP 확인 차단(401 IP check failure). 11:30 에 풀렸고
그 뒤로는 회차마다 블로그·카페가 정상으로 나가고 있다. 그런데 계기판은 하루치 누적으로 세고 있어서,
장애가 끝난 뒤에도 '0편 회차 6'이 점수 18.0 으로 일감표 1번을 하루 내내 차지했다 —
이미 끝난 일을 회차마다 다시 파게 만드는 자리였다.
`health.py` 를 고쳐 **마지막으로 발행에 성공한 회차 이후**의 0편만 센다. 지금 값 6 → 0.
누적 수치는 '어떻게' 칸에 같이 적어 둬 사라지지 않게 했다. 지금 고장 난 것만 1번에 오른다.

**고친 것 ②(일감표 2번) — 롱폼 세 칸 오차가 사실상 한 칸만 보고 있었다.**
`band_err` 는 칸마다 그 칸의 목표로 나눴다. 경쟁 롱폼 `text_bot` 목표는 실측 **0.0** 인데
`max(0, 1e-3)` 이 분모가 되면서 그 한 칸이 전체 오차의 **91.5%**를 차지했다
(우리 0.0729 / 0.001 = 72.9, 전체 79.66. 나머지 두 칸은 합쳐서 6.76).
게다가 `text_bot` 은 목표가 0이라 `if want and ...` 에서 걸러져 **'남은 차이' 목록에는 아예 안 뜬다** —
보고서에는 안 보이는데 목적함수는 그것만 보고 있었다. long.text_mid 가 82회차부터 안 닫힌 진짜 이유다.
세 칸을 같은 자로 나누게 고쳤다(그 판형 목표 세 칸의 평균).

**고친 것 ③ — white 와 text_mid 가 손잡이 하나를 놓고 싸우고 있었다.**
롱폼 흰 면적을 늘리는 길은 `text_scale`(글자 크기) 하나뿐이라 RULE 이 그것을 한계 1.0 에 못 박고 있었다.
white 차이는 상대 32%인데 text_mid 차이는 상대 638%다 — 작은 쪽이 이겨서 글자가 가운데 띠를 뒤덮었다.
롱폼 white 에서 손잡이를 떼고, `text_scale` 을 tune_bands 격자에 넘겼다(BAND_KNOB2['long']).
손잡이를 뗀 자리는 조용히 넘기지 않고 '한계 확정'으로 회차마다 이유와 함께 보고한다.

**잰 결과(106회차, 실측).** long.text_scale 1.0 → 0.55, text_y 0.5 → 0.35.

| 칸 | 경쟁 목표 | 고치기 전 | 고친 뒤 | 빗나간 폭 |
|---|---|---|---|---|
| text_top | 0.1309 | 0.0810 | 0.1778 | 0.0499 → 0.0469 |
| text_mid | 0.0784 | 0.5784 | 0.1788 | **0.5000 → 0.1004** |
| text_bot | 0.0 | 0.0729 | 0.1453 | 0.0729 → 0.1453 |
| 세 칸 절대 오차 합 | — | 0.6228 | **0.2926** | 절반 아래 |

**자를 바꿨으니 숫자를 골라 쓰지 않는다.** 회차 출력의 "경쟁 판형 거리 80.83 → 5.20"은
대부분 분모를 고친 몫이다. 같은 그림을 **옛 자**로 다시 재면 79.64 → 146.91 로 오히려 오른다
(옛 자가 text_bot 을 1000배로 세기 때문이다). 자와 무관하게 말할 수 있는 것은 위 표의 마지막 줄뿐이다 —
세 칸에서 빗나간 폭의 합이 0.6228 → 0.2926 으로 절반 아래가 됐다.

**새로 생긴 차이 하나는 배경 탓이고 글자 손잡이로는 못 닫는다.** text_bot 이 0.0729 → 0.1453 으로 늘었다.
글자를 줄이니 그동안 큰 글자와 어두운 패널에 덮여 있던 **히트맵 자체의 종목 이름(AAPL·SPCX·AVGO…)**이
아래 칸에 드러난 것이다. 경쟁 롱폼은 사진이라 아래 칸 글자가 0.0 이다. 우리 배경은 자료 화면이고
사장님 2026-09-23 지시("자료 화면이 보여야 한다")가 그걸 요구한다 — 숫자를 맞추자고 자료를 지울 자리가 아니다.

**화면 검증도 했다(식 검증으로 끝내지 않았다).** 같은 배경·같은 문구로 text_scale 1.0/0.66/0.55/0.45 를
그려 480px(실제 피드 크기)로 견줬다. 0.45 는 두 줄이 히트맵 글자에 묻혀 안 읽힌다. 숫자로는 0.45 가
더 나은데(오차 4.11 vs 4.98) 거기까지 안 내려간다 — 하한 0.55 를 thumb.py 와 KNOB_LIMIT 양쪽에 박았다.
덤으로 얻은 것: 1.0 에서는 글자 두 줄이 히트맵을 통째로 가렸는데, 0.55 에서는 자료 화면이 다 보인다.

**우리 콘텐츠 성과는 이번에도 영상으로는 판정할 수 없다.** ytupload stats: 구독 39 · 총조회 3535
(지난 회차와 같음, 0.2시간 동안 +0). 최근 10편이 **전부 private** 이라 조회가 영영 0이다.
썸네일을 아무리 고쳐도 효과를 잴 길이 없다 — 이건 루프가 못 여는 자물쇠다(사장님만 공개로 바꿀 수 있다).

**블로그(14:08 측정).** 오늘 방문 27 / 누적 11,213(어제보다 +37). 12:08 23 → 14:04 26 → 14:08 27 로
완만히 오르고 있다. 같은 시각끼리만 견준다.
perf 12: 9/24~9/25 발행 12편 **전부 당일 색인 안 됨 · 제목 검색 12/12편 30위 밖**.
지난 글 다시 재기에서는 5편 중 4편(80%)이 뒤늦게 색인됐고 그중 **3편이 1위, 1편이 3위**다
(코픽스 9월 공시 1위 · 10월 배당 일정 1위 · 근로장려금 2026 1위 · 파이어 나이 계산 3위).
"색인은 실패가 아니라 늦는 것"이 사흘 연속 같은 모양이다 — 당일 색인율로 겁먹지 않는다.
바뀐 것 없음: 어제와 같은 80%, 같은 1위 묶음. 하루치로 단정할 근거는 아직 없다.

**이 회차에서 못 한 것(지어내지 않는다).** 아래 셋은 손도 못 댔다. 시간은 롱폼 격자를 손잡이
값마다 실제로 그려 재는 데(6점 × 2회 + 확인 렌더) 대부분이 갔다.

먼저 바로잡는다 — 이 문단을 처음 적을 때 "예산 25분을 넘겨 58분을 썼다"고 적었는데 틀렸다.
회차 시계를 안 보고 중간에 찍힌 방문자 측정 시각(14:08)으로 지금이 14:50쯤이려니 짐작해서 쓴 숫자다.
beat.py 실측은 **13:52~14:10, 18.1분**으로 예산 안이다. 짐작한 숫자를 실측인 것처럼 적지 않는다.
- ③ 경쟁 제목·구성 재분석(하루 한 번) — 안 함
- ⑤ 새 무료 도구 조사(하루 한 번) — 안 함. 12:30 보고 회차가 오늘 한 번 돌렸고 새것이 없었다
- ⑥ 숏폼 한 편 생산 — 안 함. 오늘치 한 편(리얼티인컴, k4oleiiaJso)은 앞 회차가 이미 올렸다
다음 회차가 ③⑤⑥부터 시작한다. 격자 재기는 이제 한 번 끝났으니 같은 값이 반복되면 빨리 끝난다.

## 2026-09-25 15:52~16:1x · loop 113회차 (개선기) — 쇼츠 세 칸 차이를 닫았다. 손잡이가 없어서가 아니라 격자가 성겨서 못 찾던 것이었다

**일감표 1번 항목([디자인] 경쟁 대비 미해결 차이 수 3 → 목표 0)을 이번 회차에 닫으러 갔다.**

### 들어갈 때의 자리
112회차가 남긴 차이 다섯 중 셋은 이미 '한계 확정'(short.white · long.white · long.contrast)이고,
격자가 못 닫은 것이 둘이었다: **short.text_bot 0.0825(목표 0.0338)** 와 long.text_mid 0.1108(0.0784).
쇼츠 세 칸 오차 합은 1.0986 에서 여러 회차째 얼어 있었고, 그 오차의 **57%가 text_bot 한 칸**에서 왔다.

### 무엇을 의심했고, 그중 무엇이 틀렸나 (지어내지 않는다)
**① 가설: lines=3 이 낡은 값이다 → 틀렸다.**
lines=3 은 옛 자(bot 목표 0.3093, 아래 칸을 *채워야* 했다)에서 나온 값이다. 새 자는 0.0338 로 정반대를
원하므로 근거가 없어졌다고 보고, **24칸을 전부 실제로 그려 쟀다**(lines 2·3 × text_y 4점 × spread 3점).
결과는 가설을 부쉈다 — lines=2 는 12칸 최선이 1.4697 로 lines=3 최선(0.9878)보다 나빴다.
두 줄이면 아래 칸이 **항상 0.0000**이고(아래 칸에 들어갈 줄이 없다) 가운데 칸도 0 아니면 0.17 로 튄다.
**되돌리지 않는다. loop.py 주석에 이 24칸 실측을 적어 뒀다** — 다음 회차가 같은 가설을 또 세우지 않도록.

**② 가설: text_y 가 쇼츠에서 안 듣는다(2026-09-24 판정) → 그때는 맞았고 지금은 틀렸다.**
그 판정도 옛 자에서 나왔다. 새 자로 다시 재니 text_y 는 아주 잘 듣는다(bot 0.0000 ~ 0.3664).
다만 **성긴 격자로는 안 보인다**. 0.20/0.35/0.44/0.50/0.62/0.72 로 훑으면 0.50(err 0.9878)이 최선으로 보인다.
0.02 간격으로 좁혀 재니 골짜기가 나왔다:
  text_y 0.38 err 1.2487 · 0.41 1.2165 · 0.44 1.2165 · 0.46 1.0153 · **0.48 0.3690** · 0.50 0.9878
0.48 한 점에서 세 칸이 전부 목표 0.01 안에 든다: top 0.1300(0.1186) · mid 0.0884(0.0788) · **bot 0.0413(0.0338)**.

**왜 이렇게 뾰족한가**(이게 이번 회차에서 배운 것): 세 칸 오차는 아랫줄 윗변이 경계선(쇼츠 1280px)을
넘느냐 마느냐로 갈린다. 손잡이가 3px만 움직여도 한 줄이 통째로 다른 칸으로 센다.
**칸으로 세는 자에서는 손잡이가 매끄럽지 않다** — 성긴 격자는 최저점을 그냥 건너뛴다.

### 그래서 고친 것 (도구 두 군데)
1. `BAND_KNOB3` 을 새로 두고 쇼츠 세 번째 손잡이로 **text_y** 를 달았다(격자 8점, 골짜기 구간은 0.02 간격).
2. `grid_pick` 에 **반쪼개기(refine)** 를 넣었다 — 최선 양옆 중간점을 재고, 최선이 안 바뀌면 멈춘다.
   손잡이 하나당 최대 4장만 더 그린다. **이건 쇼츠 전용이 아니라 모든 격자 손잡이에 적용된다**
   (실제로 이번 회차에 text_spread 7→9점 · split_scale 6→8 · long.text_y 6→8 · text_scale 5→7 로 늘었다).
   앞으로 "격자 N점 중 이게 가장 작다"는 말이 '격자가 성겨서'가 아니라 진짜 수렴을 뜻하게 된다.

### 고친 뒤 다시 돌려 숫자로 확인했다 (113회차, 235초)
  · short.text_y 0.5019 → 0.48 (**세 칸 오차 합 1.0986 → 0.369**)
  · **경쟁 판형 거리 4.3759 → 3.6463 (줄었다)**
  · 남은 차이: **long.text_mid 하나뿐**(112회차에는 둘이었다). short.text_bot 은 목록에서 사라졌다.
루프가 스스로 "다음 회차가 바뀐 값으로 다시 그려 잰다"고 적었으므로, 114회차가 같은 숫자를 다시 확인한다.

**화면 검증도 했다(식 검증으로 끝내지 않았다).** 배경 3종 위 쇼츠 세 장을 270x480(피드 크기)으로 나란히 놓고 봤다.
세 줄이 다 읽히고 강조색도 산다. 다만 0.5019 → 0.48 은 **3px 차이라 눈으로는 같은 그림**이다 —
"화면이 좋아졌다"고 말하지 않는다. 화면 검증이 말해 주는 것은 "숫자를 맞추느라 글자를 죽이지 않았다"까지다.

### 우리 콘텐츠 성과 (지난 회차 대비)
- **영상: 이번에도 판정 불가.** 구독 39 · 총조회 3535(0.2시간 동안 +0). 최근 10편이 **전부 private**.
  썸네일을 아무리 고쳐도 조회로는 잴 길이 없다 — 루프가 못 여는 자물쇠다(사장님만 공개로 바꾼다).
  그래서 이번 회차도 '경쟁 판형 거리'(4.3759 → 3.6463)를 판정 눈금으로 썼다.
- **블로그: 올랐다.** 오늘 방문 27(14:08) → **32**(15:58), 누적 11,213 → **11,218**(어제보다 +42).
  같은 시각끼리가 아니므로 "하루 방문이 늘었다"고는 말하지 않는다 — 오늘 안에서 우상향이라는 것까지다.
- **색인: 어제와 같은 모양.** perf 12 → 9/24~9/25 발행 12편 중 11편이 당일 색인 안 됨 · 제목 검색 11/12편 30위 밖.
  지난 글 다시 재기에서는 5편 중 4편(80%)이 뒤늦게 색인됐고 **3편이 1위, 1편이 3위**
  (코픽스 1위 · 10월 배당 일정 1위 · 근로장려금 1위 · 파이어 나이 계산 3위). **나흘 연속 같은 80%다.**
  "색인은 실패가 아니라 늦는 것"이 또 확인됐다. 하루치로 단정하지 않는다.

### 경쟁 제목 재분석 (③) — 했다. series-plan.md 맨 위에 적었다
경쟁 수집이 오늘 11:56~11:58에 새로 돌아서 같은 자로 다시 쟀다(459편 그대로, 조회는 갱신).
**실제로 달라진 것은 하나**: 쇼츠 제목 길이의 상·하위 차이가 사라졌다(34자 vs 29자 → **30자 vs 30자**).
09-24 회차가 그 차이를 근거로 넣은 "숏폼 제목 길이 28~40자" 규칙을 폐기했다 — 길이는 손잡이가 아니다.
경고·부정 칸은 09-24 회차가 정규식을 안 남겨 **같은 자인지 확인할 수 없어 '견줄 수 없음'으로 표시**했다
(오늘 쓴 식을 series-plan.md 에 적어 뒀다). 달라졌다고 단정하지 않는다.

### 이 회차에서 못 한 것 (지어내지 않는다)
- ⑤ 새 무료 도구 조사 — **안 했다.** 시간이 격자 재기(24칸 + 6점 + 확인 루프 2회)에 갔다.
- ⑥ 숏폼 한 편 생산 — **안 했다. 그리고 안 한 이유로 내가 적은 숫자가 틀렸다.**
  "회차 시계가 33분이라 남은 7분에 안 들어간다"고 적었는데, **beat.py 실측은 15:52~16:13 · 20.6분**이다.
  회차 시계를 안 보고 중간에 찍힌 측정 시각으로 '지금쯤 16:25이려니' 짐작해서 쓴 숫자다.
  **바로 앞 회차(112)가 똑같은 실수를 하고 똑같이 바로잡았는데 내가 또 했다.** 시계는 짐작하지 않는다 —
  beat.py 를 읽는다. 실제로는 19분쯤 남아 있었으므로, 못 한 진짜 이유는 시간이 아니라
  **내가 시계를 안 봤다**는 것이다. 이 줄을 지우지 않고 남긴다.
  **오늘 쇼츠는 앞 회차가 한 편 올렸다**(k4oleiiaJso 리얼티인컴, 2026-09-25). 오늘치가 0편은 아니다.
  다음 회차가 ⑤⑥부터 시작한다 — 격자 재기는 이제 반쪼개기가 붙어 값이 안정되면 빨리 끝난다.

**114회차에게 넘기는 것 (순서대로)**
1. ⑥ 숏폼 한 편 — plan_2026-09-25.md 의 글감 중 '배당 ETF 수요 3.4배'(vidIQ 실측)가 자료 화면을 만들기 쉽다
   (chartimg.py bar 로 검색수 막대). 대본은 **내가 쓴다**(제미나이 아님 — 사장님 2026-09-23).
2. ⑤ 새 무료 도구 조사 — 이틀째 안 됐다. 세 회차 연속이면 tools-wanted.md 에 올린다.
3. text_y 0.48 이 다음 회차 재측정에서도 유지되는지 확인. 뾰족한 골짜기라 값이 튀면 측정 쪽을 의심한다.


## 2026-09-25 18:20 · 119회차 (루프 자가개선)

**① 일감표 4번 [디자인] 경쟁 대비 미해결 차이 수 — 숫자는 크게 줄었고, 항목 자체는 못 닫았다.**
경쟁 판형 거리 **3.5737 → 1.5935**, 롱폼 세 칸 오차 **3.3034 → 1.2245**. 그래도 `long.text_mid` 는
남아 있으므로(0.1389 vs 0.0784) 일감표 항목은 `막힘`으로 적었다. 줄인 것을 닫은 것처럼 쓰지 않는다.

**무엇을 고쳤나 — 아래 칸(text_bot)이 진짜 범인이었는데 차이 목록에 안 보였다.**
롱폼 세 칸 오차 3.2773 의 **75%가 아래 칸 하나**였다(우리 0.1719 · 경쟁 0.0000). 그런데 경쟁 목표가 0 이라
`if want and ...` 조건에서 통째로 빠져서 '남은 차이' 목록에는 한 번도 안 떴다. 보고서에는 text_mid 만 보이고
격자는 사실상 text_bot 을 줄이고 있었다. **눈에 보이는 차이가 제일 큰 차이가 아니었다.**
- 갈라 재서 원인을 찾았다: 같은 배경·같은 설정에서 **글자만 빼고** 그리면 아래 칸이 **0.0000** 이다.
  즉 아래 칸 잉크는 (글자가 거기 있으면) 글자이고, 글자를 위로 올리면 글자 뒤 어두운 띠도 같이 올라가
  이번엔 배경(히트맵)의 결이 그대로 드러나 0.14 가 된다. `text_y` 6점 × `text_scale` 3점 **18장을 전부 그려**
  재 봤고 아래 칸 최저가 0.0845 였다 — 글자 손잡이로는 못 닫는 차이다.
- 그래서 **`bot_scrim`**(아래에서 몇 %를 그라데이션으로 눌러 평평하게, thumb.py)을 새로 달았다.
  잘라 내지 않고 위는 투명·아래로 갈수록 진하게라 자료 화면이 사라지지 않는다.

**② 손잡이를 달았는데도 안 움직였다 — 좌표 하강의 구조적 한계를 실측으로 확인했다(118회차).**
`bot_scrim` 을 달고 두 바퀴를 돌렸는데 3.2773 → **3.2047**, 0.07 밖에 안 줄었다. 이유:
첫 바퀴에 scrim 을 고를 때 **글자가 아직 아래(text_y 0.72)에 있어** 아래 칸 잉크가 배경이 아니라 글자다.
글자는 scrim 위에 그리므로 그 자리에서 scrim 의 이득은 0 이고(격자 전 구간 3.26~3.31 로 평평), 0.1 이 뽑혔다.
**둘을 같이 움직여야만 보이는 자리**였다(24장 전수 실측, text_scale 0.55):
| | text_y 0.72 | text_y 0.35 |
|---|---|---|
| scrim 0.00 | 3.4919 | 4.1839 |
| scrim 0.38 | 3.2631 | **2.0042** |
혼자 움직이면 둘 다 손해거나 본전인데 같이 움직이면 -2.18 이다. 그래서 `joint_seed` — 롱폼만
좌표 하강 **앞에** 8점짜리 거친 2차원 훑기를 한 번 두게 고쳤다(loop.py). 119회차 실측:
`text_y 0.67→0.35 · bot_scrim 0.1→0.38` 로 3.3034 → 2.2127, 이어서 좌표 하강이 0.3875 · 0.775 로 다듬어 **1.2245**.
세 칸: top 0.1771 · mid 0.1866 · **bot 0.0000**(경쟁 0.0).

**③ 화면 검증(숫자만 보고 안 넘어간다).** 480px(실제 피드 크기)로 새 판형과 옛 판형을 나란히 봤다.
두 줄 다 읽히고, 제목이 아래(0.72)에서 위(0.3875)로 올라가 경쟁 롱폼 구조에 가까워졌다.
아래 그라데이션 밑에서도 히트맵 칸이 보인다 — 자료가 사라지지 않았다.

**④ 새로 생긴 대가 — 찾았고, 이번 회차 안에 되돌렸다.** scrim 을 230 으로 누르니 그림이 어두워져
`long.bright` 0.3315(경쟁 0.4797)·`long.dark` 0.5147(경쟁 0.4104)이 **새 차이로 떴다**. 차이 하나를 닫고
둘을 연 것이라, 계기판의 '미해결 차이 수'로는 1 → 3 으로 **나빠진 것이 맞다**. 그대로 두지 않았다.
누르는 넓이(`bot_scrim`)와 누르는 세기(`scrim_a`)를 갈라 손잡이 둘로 만들고 세기를 실측했다
(같은 배경·같은 문구, 110/150/190/230):
| scrim_a | 세 칸 오차 | 아래 칸 | bright(경쟁 0.4797) | dark(경쟁 0.4104) |
|---|---|---|---|---|
| 110 | 1.4910 | 0.0074 | **0.3999** | **0.3592** |
| 150 | 1.3592 | 0.0033 | 0.3770 | 0.4160 |
| 190 | 1.3254 | 0.0011 | 0.3542 | 0.4646 |
| 230 | **1.2245** | 0.0000 | 0.3315 | 0.5147 |
**110 을 골랐다.** 아래 칸은 110 에서도 0.0074 로 이미 '차이 없음'(0.01 미만)이고, 더 눌러 얻는 0.007 은
밝기 두 항목을 내주고 살 값이 아니다. 110 에서 bright·dark 둘 다 25% 안으로 들어와 **차이 목록에서 빠진다**.
최종 상태(재측정): 롱폼 세 칸 **1.4910** · top 0.1568 · mid 0.1491 · bot 0.0074 · bright 0.3999 · dark 0.3592.
남는 차이는 `long.text_mid` 하나 — **들어올 때와 같은 1개**이고, 판형 거리는 3.5737 에서 내려왔다.

**⑤ 우리 성과(하루치로 단정하지 않는다).**
- 블로그 방문 오늘 36 · 전체 11,222(어제보다 +46). 17:40 계기판의 32 에서 두 시간 새 +4.
- 색인: 최근 12편 중 11편이 아직 색인 안 됨(어제 것 포함). 대신 **지난 글 5편 중 4편(80%)이 뒤늦게 색인**됐고
  그중 3편이 제목 검색 **1위**(코픽스·10월 배당 일정·근로장려금), 파이어 계산 글이 3위.
  → 색인은 느리게 되고 있다. **오늘 편수보다 며칠 뒤 순위를 봐야 하는 지표**다.
- 유튜브: 구독 39 · 총조회 3535(+0). **최근 10편이 전부 private** 이라 조회로는 아무것도 판정 못 한다.
  이건 루틴이 못 푸는 항목이고 계기판에도 '사장님만 할 수 있는 것'으로 올라가 있다.

**⑥ 못 한 것.** 제목 패턴 재분석(③)·새 도구 조사(⑤)·숏폼 생산(⑥)은 **안 했다**. 회차 25분 안에
①②가 다 들어가지 않았다(루프를 두 번 돌렸다 — 424초 + 491초). 숏폼은 어제 회차부터 **세 번째 밀렸다**.
다음 회차는 ⑥부터 시작한다.

**120회차에게 넘기는 것 (순서대로)**
1. ⑥ 숏폼 한 편 — 세 회차 연속 밀렸다. 이것부터 한다. 대본은 **내가 쓴다**(제미나이 아님).
2. `scrim_a` 는 지금 사람이 재서 고른 고정값(110)이다. 자동 격자에 붙이지 않았다 —
   세 칸 오차만 보는 격자는 230 을 골라 밝기를 도로 망가뜨린다(위 표가 그 증거다).
   붙이려면 밝기까지 같이 보는 목적함수가 먼저 있어야 한다. 그 전엔 손대지 않는다.
3. `long.text_mid` 0.1389 vs 0.0784 — 두 줄짜리 한글 제목으로는 top 과 mid 를 동시에 못 맞춘다
   (우리 글자 잉크가 경쟁의 약 1.7배). 닫으려면 글자를 더 줄여야 하는데 `text_scale` 하한 0.55 는
   화면 검증에서 나온 값이다. **숫자를 맞추려고 글자를 죽이지 않는다** — 제목 길이 쪽을 먼저 보라.


---

## 2026-09-25 19:52~20:20 (자가개선 루프 **121·122회차**)

**① 일감표 1번을 닫았다 — `long.text_mid`.** 들어올 때 계기판의 '미해결 차이'는 1개였고 그게 이 항목이었다.

**왜 120회차까지 못 닫았나.** 119회차가 넘긴 쪽지에는 "두 줄짜리 한글 제목으로는 top 과 mid 를 동시에
못 맞춘다, 닫으려면 글자를 더 줄여야 하는데 `text_scale` 하한 0.55 는 화면 검증값이다"라고 적혀 있었다.
사실이었지만 **이유가 한 칸 얕았다**. 진짜 이유는 하한이 아니라 **두 줄이 손잡이 하나를 나눠 쥐고 있던 것**이다.
120회차 격자표(bands.long)가 그대로 보여 준다:

| text_y | text_top (목표 0.1309) | text_mid (목표 0.0784) |
|---|---|---|
| 0.20 | 0.3890 | 0.0275 |
| 0.3875 | 0.1558 | 0.1389 |

두 줄을 다 위로 올리면 mid 는 비는데 top 이 목표의 3배가 되고, top 을 맞추면 아랫줄이 통째로 가운데 칸에 앉는다.
`text_scale` 로 줄이면 **두 줄이 같이** 작아져 top 도 같이 무너진다. 판형 오차 1.2245 의 **71%가 mid 한 칸**이었다.

**단 것: `sub_scale` — 아랫줄만 작게** (thumb.py 손잡이 + loop.py `BAND_KNOB4`).
윗줄은 1px도 안 움직인다 — `ys[1] = cy` 가 윗줄 높이로만 정해지기 때문이다. 그래서 top 을 지키고 mid 만 내린다.
큰 제목 + 작은 부제는 경쟁 롱폼이 실제로 쓰는 배치라 판형을 지어낸 것이 아니다.

**실측 4점**(같은 배경·같은 문구):

| sub_scale | 세 칸 오차 | text_top | text_mid |
|---|---|---|---|
| 1.0 | 1.2245 | 0.1684 | 0.1264 |
| **0.9** | **0.5297** | 0.1673 | **0.0778** |
| 0.8 | 1.2170 | 0.1684 | 0.1258 |
| 0.7 | 0.8611 | 0.1673 | 0.1021 |

0.9 에서 mid 가 **0.0778**, 목표 0.0784 에 붙었다. (0.8 이 되레 나쁜 것은 아랫줄 윗변이 칸 경계를 넘나드는
뾰족한 골짜기 탓이다 — 113회차 쇼츠 `text_y` 에서 본 것과 같은 모양이라, 격자에 0.9 를 넣고 반쪼개기에 맡겼다.)

**122회차가 기계로 다시 확인했다**(내가 손으로 잰 값이 아니라 루프가 스스로 고른 값):
`long.sub_scale 1.0 → 0.9 (세 칸 오차 합 1.2245 → 0.5297, 격자 6점 실측)` ·
**경쟁 판형 거리 1.5935 → 0.8987 (줄었다)**.

**화면 검증도 했다**(숫자만 보고 넘기지 않는다). 1280x720 을 480px 로 줄여 실제 피드 크기로 봤다 —
윗줄 92px · 아랫줄 82px 로, 아랫줄이 작아진 것이 눈에 보이지만 히트맵 위에서 또렷이 읽힌다.
읽힘 바닥은 thumb.py 가 지킨다: 아랫줄도 자연 크기의 0.55(화면 검증으로 정한 바닥) 밑으로는 안 내려간다.

**남은 차이는 1개, 0 이 아니다.** `long.text_top` 0.1673 vs 0.1309(상대 29%) — 윗줄이 목표보다 굵다.
121회차에 이 항목이 새로 떴는데, 없던 것이 생긴 게 아니라 **mid 뒤에 가려 있던 것**이다.
이제 두 칸이 손잡이를 나눠 쥐지 않으므로 다음 회차 격자가 `text_scale` 을 윗줄 쪽으로 다시 고를 수 있다.

**② 락이 고장 나 있었다 — 루프가 겹쳐 돌 수 있는 상태였다.** 이번 회차에 실제로 당해서 찾았다.

```python
if __name__ == '__main__':
    try: main()
    finally: drop_lock()      # ← main() 이 '락을 못 잡아 그냥 돌아온' 경우에도 실행된다
```

`take_lock()` 이 실패하면 `main()` 은 바로 돌아오는데, `finally` 는 그래도 `drop_lock()` 을 부른다.
즉 **못 들어간 바퀴가 나가면서 돌고 있는 바퀴의 락을 지운다.** 그러면 그다음에 오는 바퀴는 문이 열린 것으로
보고 그대로 들어와 같은 샘플 파일에 겹쳐 그린다 — 락을 만든 이유였던 2026-09-24 사고(두 바퀴가 겹쳐 돌아
격자 측정이 섞이고 손잡이를 0.45→0.0 으로 잘못 옮겨 판형 오차가 0.2653 → 2.213 으로 뛴 일)가 그대로 재발할 수 있었다.

실측: 내 바퀴가 **20:04:43** 에 락을 잡고 돌고 있었는데 **20:11:56** 에 락 파일이 사라졌다
(그 시각 selfloop 이 worklist 를 다시 만들었다 — selfloop 은 `loop.py` 를 부른다).
그때 살아 있던 프로세스는 내 `loop.py`(PID 915500)와 그 자식 `thumb.py` 뿐이었다.
**그 바퀴가 그리지는 않았으므로 이번 측정은 안 섞였다** — 운이 좋았던 것이지 막혀 있던 것이 아니다.

고쳤다: `HELD` 플래그를 둬서 **내가 잡은 락만 푼다**.

**③ 제목 규칙 재분석 — 안 했다(할 것이 없다).** 경쟁 meta.json 12개가 전부 **오늘 11:56~11:58** 수집분이고
(12채널 459편) 제목 규칙 재측정은 **오늘 16:2x(113회차)** 에 그 말뭉치로 이미 돌았다.
말뭉치가 그대로이므로 다시 세도 같은 값이 나온다. 수집은 12시간 문에 걸려 이번에도 건너뛰었다.

**④ 우리 성과(하루치로 단정하지 않는다).**
- 블로그 방문 **오늘 42** · 전체 11,228(어제보다 +52). 19:53 계기판의 36 에서 30분 새 +6.
- 색인: 최근 12편 중 **10편이 아직 색인 안 됨**. 대신 **지난 5편 중 4편(80%)이 뒤늦게 색인**됐고
  그중 3편이 제목 검색 **1위**(코픽스·10월 배당·근로장려금), 파이어 계산 글 3위.
  오늘치 글로는 판정하지 못한다 — 며칠 뒤 순위로 봐야 하는 지표다. 어제와 같은 결론이다.
- 유튜브: 구독 39 · 총조회 3535 → **3545**(회차 사이 +10). **최근 10편이 전부 private** 이라
  조회로는 여전히 아무것도 판정 못 한다. 늘어난 10회는 옛 공개 영상 몫이다.
  그래서 이 회차의 판정 눈금은 조회가 아니라 **경쟁 판형 거리**다(위 ①).

**⑤ 새 도구 조사 · ⑥ 숏폼 생산 — 이번 회차에는 안 했다.** 둘 다 **오늘 이미 한 번씩 했다**:
도구 조사는 04:1x(한국어 오프라인 TTS MeloTTS — 빌드 도구가 없어 이 PC에는 못 깖, README-tools.md 32줄),
숏폼은 04:1x 에 `nw40`(40대 순자산) 한 편을 만들어 올렸다. 하루 한 번이면 되는 일이라 건너뛰었다.

**123회차에게 넘기는 것 (순서대로)**
1. `long.text_top` 0.1673 vs 0.1309 — **이제 손잡이가 안 겹친다.** `text_scale` 격자를 다시 돌려 보라.
   내리면 윗줄이 얇아지고, 아랫줄은 `sub_scale` 이 따로 쥐고 있으니 mid 가 같이 무너지지 않는다.
   단 `text_scale` 하한 0.55 는 화면 검증값이라 그 아래로는 안 내려간다.
2. `long.bot_scrim` 이 그리기 한계 0.5 에 붙은 채 두 회차째 '막힘'으로 뜬다. 세 칸 오차는 이미
   bot 0.0000 이라 더 벌 것이 없는데 한계 경고만 나온다 — 한계를 넓힐 것이 아니라 **경고를 끄는 것**이 맞다.
   (`bot` 이 이미 목표면 끝점 경고를 내지 않게.)
3. 락 고침은 이 회차 것만 고쳤다. **다른 스크립트에도 같은 `try/finally: drop_lock()` 꼴이 있는지** 한 번 훑어라.

---

# 125회차 2026-09-25 22:13 (자가개선 루프) — **미해결 차이 0**. 거짓 막힘 하나, 조용한 고장 둘을 닫았다

**① 회차 본업(세 칸 맞추기).** 경쟁 판형 거리 **0.8987 → 0.8822**(줄었다). 롱폼 세 칸 오차 **0.5297 → 0.5132**
(`long.text_y` 0.3688 → 0.38, 격자 11점 실측). 쇼츠는 0.369 그대로(전 손잡이 유지).
**미해결 차이 0** — 남은 넷은 전부 '한계 확정'이다(long.contrast · long.text_top · long.white · short.white).

**② 122회차가 넘긴 2번을 닫았다 — 넓힐 것이 아니라 경고를 끄는 것이 맞았고, 이제 근거가 실측이다.**
`long.bot_scrim` 이 한계 0.5 에 붙어 두 회차째 "손잡이를 더 넓혀야 줄어든다"로 떴다. 정말 그런지 재 봤다.
thumb.py 한계를 0.5 → 0.8 로 열고 같은 설정(text_y 0.3688 · text_scale 0.775 · sub_scale 0.9, 배경 3종 중앙값)에서 5점을 그려 쟀다:

| bot_scrim | 세 칸 오차 | text_top | text_mid | text_bot | bright | contrast |
|---|---|---|---|---|---|---|
| 0.50 | 0.5297 | 0.1673 | 0.0778 | 0.0000 | 0.3881 | 0.2597 |
| 0.55 | 0.5297 | 0.1673 | 0.0778 | 0.0000 | 0.3820 | 0.2571 |
| 0.60 | 0.5297 | 0.1673 | 0.0778 | 0.0000 | 0.3775 | 0.2553 |
| 0.70 | 0.5297 | 0.1673 | 0.0778 | 0.0000 | 0.3701 | 0.2531 |
| 0.80 | 0.5297 | 0.1673 | 0.0778 | 0.0000 | 0.3644 | 0.2521 |

세 칸이 **1픽셀도 안 움직인다**. 아래 칸은 0.5 에서 이미 목표(0.0)에 정확히 닿아 더 벌 것이 없고,
남은 오차는 전부 `text_top`(잔차의 98%) 인데 그 칸은 이미 실측 35점으로 한계 확정된 자리다.
대신 `bright` 가 0.3881 → 0.3644 로 **목표 0.4797 에서 더 멀어지고** contrast 도 0.2597 → 0.2521 로 멀어진다.
즉 넓히면 밝기·대비만 내주고 얻는 것이 0 이다. **thumb.py 한계는 0.5 로 되돌렸다.**
loop.py 는 이제 한계에서 무조건 경고하지 않는다 — 남은 오차의 10% 이상을 차지하는 칸이 격자 윗구간에서
이미 평평(<0.002)하면 '막힘'이 아니라 '넓혀도 소용없다'로 기록만 남긴다. 이번 회차 출력에 그렇게 찍혔고,
그 덕에 `blocked` 가 비면서 계기판의 미해결 차이가 0 이 됐다.

**③ 이 회차가 실제로 겪어서 찾은 조용한 고장 둘 — 둘 다 아무도 안 알려 주던 것이다.**

- **격자를 훑다 죽으면 design.json 이 '재보던 값'에 남는다.** 앞 회차를 300초에서 끊었더니 `long.text_y` 가
  고른 값 0.3688 이 아니라 격자 점 **0.72** 에 남아 있었다(세 칸 오차 0.5297 → **2.5731**). 커밋된 값과
  달라진 것을 `git diff` 로 보기 전까지 몰랐고, 그동안 나가는 썸네일은 그 값으로 그려진다.
  → `grid_pick` 이 격자를 훑는 동안 `design.json` 에 `_probe`(되돌릴 자리)를 같이 적고, 다음 회차 `main` 이
  그것을 보고 고른 값으로 되돌린다. **이번 회차 출력 첫 줄에서 실제로 작동했다**:
  "앞 회차가 long.text_y 격자를 훑다 죽었다 — 재보던 값 0.72 를 고른 값 0.3688 로 되돌렸다".
- **죽은 락이 한 시간 동안 회차를 통째로 막는다.** `take_lock` 이 락 파일의 **나이만** 보고 주인이 살아 있는지
  묻지 않았다. 죽은 PID 753288 이 남긴 락 때문에 이번 회차가 "다른 바퀴가 604초째 돌고 있다"로 그냥 돌아갔다.
  두 시간마다 도는 회차라 한 번 죽으면 다음 회차까지 통째로 날아간다.
  → 주인 PID 가 살아 있는지 `naverpost._alive` 로 묻고(두 군데서 다르게 세지 않으려고 그것을 그대로 쓴다),
  죽었으면 치우고 들어간다. 이번 회차가 그 길로 들어왔다: "죽은 락을 치운다 — 주인 753288 는 살아 있지 않다".

**④ 우리 성과(하루치로 단정하지 않는다).**
- 블로그 방문 **오늘 48** · 전체 11,234(어제보다 +58). 21:39 회차의 42 에서 25분 새 +6.
- 색인: 최근 12편 중 10편이 아직 색인 안 됨. 다시 잰 5편 중 **4편(80%)이 뒤늦게 색인**됐고 그중 3편이 제목 검색 1위.
  오늘 글로는 판정하지 못한다 — 어제·앞 회차와 같은 결론이다.
- 유튜브: 구독 39 · 총조회 **3545 그대로**(회차 사이 +0). 최근 10편이 전부 private 이라 조회로는 판정 못 한다.
  그래서 이 회차의 판정 눈금도 조회가 아니라 경쟁 판형 거리다(위 ①).

**⑤ 제목 규칙 재분석 — 안 했다(할 것이 없다).** 경쟁 meta.json 12개가 여전히 오늘 11:56~11:58 수집분이고,
제목 규칙 재측정은 오늘 16:2x(113회차)에 그 말뭉치로 이미 돌았다. 말뭉치가 그대로라 다시 세도 같은 값이다.

**⑥ 새 도구 조사 · ⑦ 숏폼 생산 — 이번 회차에는 못 했다.** 회차 예산 25분을 ①~③에 다 썼다
(죽은 락 때문에 loop.py 를 두 번 돌려야 했고, 그리기 실측 5점 + 본 회차 격자로 약 18분이 나갔다).
도구 조사는 오늘 04:1x 에 한 번 했다(MeloTTS, README-tools.md). 숏폼은 오늘 04:1x `nw40` 이후 안 만들었으므로
**다음 회차의 1순위**다 — 재료는 오늘 발행한 오피스텔 수익률 글(work/research/offitbl0925/pkg/facts.txt,
국토부 실거래 798곳·수익률 중앙값 5.13%)이 그대로 쓸 수 있다. 지어내지 말고 그 facts 로 쓸 것.

**126회차에게 넘기는 것 (순서대로)**
1. **숏폼 한 편.** 위 ⑦의 재료로. 이틀째 안 만들었다.
2. 122회차가 넘긴 3번이 아직 남았다 — **다른 스크립트에도 `try/finally: drop_lock()` 꼴과 '나이만 보는 락'이
   있는지 훑어라.** 이번에 loop.py 의 '나이만 보는 락'을 고쳤는데, 같은 꼴이 watchdog·selfloop 쪽에도 있는지
   확인 안 했다(확인 안 함이라고 적는다).
3. 롱폼 세 칸은 0.5132 에서 좌표 하강 바닥이다. 남은 것은 전부 '배경을 무엇으로 까나'이고 그리기 손잡이가
   아니다 — tools-wanted.md 에 사람 판단으로 올라가 있다. 손잡이를 더 달지 마라.

## 2026-09-25 23:46~2026-09-26 00:0x · loop **126회차** (개선기) — 숏폼 한 편 만들었다. 픽셀 루프는 바닥에 닿았다

**① 이번 회차가 닫은 것: 125회차가 넘긴 1번(숏폼).** 이틀째 안 만들던 것을 만들었다.
- 재료는 넘겨받은 그대로 `work/research/offitbl0925/pkg/facts.txt`(국토부 실거래 798곳, 수익률 중앙값 5.13%).
- 자료 화면 5장을 `chartimg.py bar --short` 로 먼저 만들었다(`work/research/_shots/short0925o/c1~c5.png`).
  글자만 있는 장면은 하나도 없다.
- **대본은 내가 썼다**(Gemini 아님, 사장님 2026-09-23 지시). `shortscript.py check` **통과**.
- 올림: https://youtu.be/LSmLBXfXlh8 (private) — "오피스텔 월세 수익률 798곳, 1위 15.48%와 꼴찌 1.08% #Shorts"
- 제목은 113회차 규칙을 따랐다: 숫자 넣고(798·15.48·1.08), 경고어·물음표 안 넣고, 길이를 맞추려고 말을 늘리지 않았다.

**② 125회차가 넘긴 2번(락) — 확인했고, 고칠 것이 없다.** "다른 스크립트에도 같은 꼴이 있나"를 실제로 훑었다.
- `grep -l "take_lock|drop_lock|\.lock" work/*.py` → **loop.py 와 naverpost.py 두 개뿐**이다. watchdog·selfloop 에는 락이 없다.
- `naverpost.acquire_lock` 은 **이미 `_alive(owner)` 를 묻고 있다**(51행). loop 가 121회차에 고칠 때 그 함수를 갖다 쓴 것이라 같은 버그가 날 수 없다.
- `loop.py` 의 `finally: drop_lock()` 은 `HELD` 를 보고 **내가 잡은 락만** 지운다(53~66행). 못 들어간 바퀴가 남의 락을 지우는 길은 막혀 있다.
- → 넘겨받은 항목 2번은 **닫는다**. 확인 안 한 것이 아니라 확인했고 없다.

**③ 픽셀 루프는 좌표 하강 바닥이다 — 이번 회차에 고칠 그림 차이가 없었다.**
`loop.py` 126회차: `남은 차이: 없음 — 남은 것은 한계 확정 long.contrast, long.text_top, long.white, short.white`.
short 0.369 · long 0.5132 · 합 **0.8822 → 0.8822(그대로)**. 손잡이 여섯 개(short 3 · long 4)를 전부 격자로 다시 훑었고
전부 "유지(가장 작다)"로 돌아왔다. 125회차가 "손잡이를 더 달지 마라"고 넘긴 것이 이번 실측으로도 맞다.
**회차 임무 2번(루프가 못 닫는 차이를 내가 닫는다)은 닫을 대상 자체가 없었다** — 지어내서 손잡이를 돌리지 않았다.

**④ 제목 규칙 재분석 — 안 했다(말뭉치가 그대로다).** 경쟁 수집이 오늘 11:58 분이고(12시간 규칙으로 건너뜀),
제목 규칙은 오늘 16:2x 113회차에 그 말뭉치로 이미 돌았다. 같은 말뭉치를 다시 세면 같은 값이 나온다.

**⑤ 우리 성과(하루치로 단정하지 않는다).**
- 블로그 방문 **오늘 53** · 전체 11,239(어제보다 +63). 125회차(21:39경) 48 → 25분 뒤 회차에서 53, **+5**.
- 색인: 오늘 12편 중 **10편 아직 색인 안 됨**(125회차와 같은 수). 잡힌 2편은 코픽스 주담대 **3위**, 오피스텔 월세 수익률 **1위**.
  다시 잰 지난 글 5편 중 **4편(80%)이 뒤늦게 색인**, 그중 3편이 제목 검색 1위. 어제·앞 회차와 같은 그림이다 —
  **오늘 글의 색인율로는 아무것도 판정하지 못한다**는 결론이 네 회차째 같다.
- 유튜브: 구독 39 · 총조회 **3545 그대로**(회차 사이 +0, 1.7시간).

**⑥ 새 도구 조사 — 못 했다.** 회차 예산을 ①(차트 5장 + 대본 + 렌더 + 업로드)과 성과 측정에 썼다.
오늘 04:1x 에 한 번 했으므로(MeloTTS) "하루 한 번"은 채워져 있다.

**⑦ 이 회차가 사장님께 올릴 것 — 루틴이 스스로 못 푸는 모순 하나.**
회차 지시서는 숏폼을 **private 으로 올리라**고 되어 있는데, 계기판은 "공개한 영상 수 0/10 — 비공개면 조회가
영영 0이라 조정 효과를 잴 수 없다"를 사장님 항목으로 띄운다. **루틴이 스스로 만든 상태를 루틴이 문제로 센다.**
오늘로 private 숏폼이 **12편**(이번 것 포함)이고 그중 조회가 1 이상인 것은 없다. 썸네일·제목 규칙을 아무리 고쳐도
**공개 전에는 조회로 검증할 길이 없어** 판정 눈금이 계속 '경쟁 판형 거리'라는 대리 지표에 머문다.
→ tools-wanted.md 에 '사람이 봐야 함'으로 올렸다.

**127회차에게 넘기는 것 (순서대로)**
1. **새 도구 조사**(회차 임무 5번). 125·126 두 회차 연속 못 했다. 무료·키 없는 것만, 실제로 깔아서 한 번 돌려 보고 README-tools.md 에 적어라.
2. 숏폼 재료는 다음 것을 새로 골라야 한다 — offitbl0925 는 이번에 썼다. 오늘 발행한 글 중 `plan_2026-09-26.md` 나 퇴직연금 수익률 39배 글이 후보다.
3. **픽셀 손잡이는 건드리지 마라.** 126회차 실측으로도 여섯 손잡이 전부 격자 최소점이다. 남은 0.8822 는 '배경을 무엇으로 까나'이고 그리기 손잡이가 아니다.

---

## 2026-09-26 02:2x — 127·128회차 (개선기 loop)

**① 이번 회차가 닫으러 간 것: `long.bot_scrim` 막힘. 닫지 못했다 — 대신 그것이 '막힘이 아님'을 실측으로 세웠다.**
127회차가 `long.bot_scrim 가 그리기 한계 0.5 에서 멈췄다 — 손잡이를 더 넓혀야 줄어든다(아직 움직이는 칸: text_mid)`를
띄웠다. 125회차가 똑같은 자리를 실측으로 닫아 두고 "회차마다 막힘으로 띄우면 다음 회차가 같은 일을 또 한다"는
주석까지 붙여 뒀는데, 그 방지 장치가 새어 나온 것이다. 그래서 두 번 쟀다.

- **thumb.py 한계를 0.7 로 임시로 열고 6점 실측**(text_y 0.38 · text_scale 0.775 · sub_scale 0.9 고정, 배경 3종 중앙값):
  0.50 / 0.525 / 0.55 / 0.60 / 0.65 / 0.70 → **세 칸 오차가 전부 0.6187**, top 0.1655 · mid 0.0796 · bot 0.0000 으로
  소수 넷째 자리까지 똑같다. 한계 위에는 아무것도 없다.
  대가만 있었다: bright 0.3888 → 0.3717(목표 0.4851 에서 **멀어진다**) · contrast 0.2606 → 0.2550(**역시 멀어진다**).
- **왜 방지 장치가 새었나.** 판정이 '남은 오차의 10% 이상'이라는 **비율만** 봤다. 판형이 좋아져 잔차가 작아지면
  아무것도 아닌 칸이 그 문턱을 넘는다. 이번 롱폼 잔차는 top 0.0359 · mid **0.0060** · bot 0.0 (합 0.0419)이고,
  mid 는 절대값으로 0.006 인데 비율로는 14.3% 라 '아직 산다'로 잡혔다. 한계 **아래**(0.45·0.475·0.5)에서
  0.0045 움직이던 기울기를 한계 **위**에 남은 것이 있다는 뜻으로 읽은 것도 틀렸다 — 아래 칸이 0.5 에서 목표 0.0 에
  정확히 닿으면서 띠가 가운데 칸에 더는 안 걸치기 때문에 거기서 끝난다.
- **고친 것**: `loop.py` 의 liveness 판정에 **절대 바닥 0.01** 을 같이 뒀다(`LIVE_FLOOR`). 근거는 세 칸 목표 평균
  (롱폼 0.0677 · 쇼츠 0.0771)이라 0.01 은 오차 0.15 어치이고, 그만큼 벌려고 미해결 차이가 0.13 인 밝기·대비를
  내주는 것은 손해라는 것이다. thumb.py 한계는 **0.5 로 되돌렸다**(재 보려고만 열었다).
- **128회차로 확인**: 같은 자리가 `한계 0.5 에 있지만 넓혀도 소용없다 — 남은 오차 0.6187 은 text_top 0.0359,
  text_mid 0.0060 에서 오고, 그 칸들은 격자 윗구간에서 이미 평평하다` 로 바뀌었다. **막힘 1건 → 0건.**
  **오차는 안 줄었다(0.6187 그대로).** 줄일 것이 없다는 것을 실측으로 확정한 것이 이번 회차의 결과다.

**② 같이 물러본 것 — `text_y` x `sub_scale` 같이 훑기. 실측이 생각을 부쉈다.**
`BAND_CAPPED` 의 long.text_top 판정이 "top 에 닿는 자리(0.40·0.41)에서 mid 가 0.04~0.07 빈다"이고,
`sub_scale` 은 아랫줄만 줄여 mid 만 만지는 손잡이로 달아 둔 것이라, 둘을 JOINT 에 같이 넣으면
'top 을 맞추러 옮기고 그때 비는 mid 를 아랫줄 크기로 메운다'가 될 것이라 봤다. **30점을 전부 그려 쟀다**
(text_y 0.38·0.395·0.41·0.425·0.44 x sub_scale 0.7·0.75·0.8·0.85·0.9·1.0):

| text_y | sub 0.7 | 0.75 | 0.8 | 0.85 | 0.9 | 1.0 |
|---|---|---|---|---|---|---|
| 0.38 mid | 0.1039 | 0.1160 | 0.1276 | 0.1418 | **0.0796** | 0.1281 |
| 0.41 mid | 0.1446 | 0.1567 | 0.1706 | 0.1825 | **0.1203** | 0.1688 |

**아랫줄을 줄이면 mid 가 오히려 커진다.** 0.9 한 점만 혼자 떨어지는 계단이고(아랫줄 윗변이 가운데 칸 경계를
넘느냐로 갈린다) 그 앞뒤는 0.10~0.18 이다. 연속으로 mid 를 줄이는 손잡이가 아니라서 JOINT 에 넣을 물건이 못 된다.
30점 최선은 **지금 자리(0.38 · 0.9, 0.6187)** 였고 2위가 0.41 · 0.9(0.7599)다 — top 을 맞추러 가면 전체가 나빠진다.
`BAND_CAPPED` 의 long.text_top 판정이 이 30점으로도 그대로 섰다. **loop.py 주석에 30점을 적어 뒀다 — 다시 넣으려면 여기서부터 다시 재라.**

**③ 경쟁 판형 거리 0.8822 → 0.9877 (늘었다). 우리가 나빠진 것이 아니라 목표가 움직였다.**
127회차가 경쟁 12채널을 새로 수집해 **썸네일 606장을 재측정**했고, design.json 은 **한 칸도 안 바뀌었다**(`changed: []`).
같은 그림을 새 자로 재니 거리가 늘어난 것이다 — 조정 실패로 읽으면 안 된다. 128회차는 같은 말뭉치라 0.9877 그대로다.

**④ 우리 성과(하루치로 단정하지 않는다).**
- 블로그 방문 **오늘 4** · 전체 11,244(어제보다 +5). 날이 바뀐 직후(02:20)라 오늘 숫자로는 아무것도 못 본다.
- 색인: 오늘·어제 12편 중 **10편 색인 안 됨**. 잡힌 2편은 코픽스 주담대 **3위**, 오피스텔 월세 수익률 **1위**.
  다시 잰 지난 글 5편 중 **4편(80%)이 뒤늦게 색인**, 그중 3편이 제목 검색 1위. **다섯 회차째 같은 그림**이다.
- 유튜브: 구독 39 · 총조회 **3545 그대로**(127→128 사이 0.3시간 +0).

**⑤ 넘기는 것 — 픽셀 루프는 지금 배울 것이 없다.**
숏폼 **11편이 전부 private** 이라 조회가 영영 0이고, 남은 차이 4건은 모두 '한계 확정'이며 이번 회차가 마지막
막힘까지 실측으로 닫았다. 즉 **경쟁 판형 거리라는 대리 지표마저 손잡이로는 더 못 줄인다.**
125·126회차가 적어 둔 결론과 같다: 남은 거리는 '배경을 무엇으로 까나'이고 그리기 손잡이가 아니다.
→ 다음 회차는 **손잡이를 만지지 마라.** 배경 자료(네이버 부동산 화면·차트 종류)를 바꿔 재거나, 픽셀이 아닌
**글·카페 축**(worklist 1~3번)으로 가라.

**⑥ 못 한 것 (지어내지 않는다)**
- **제목 규칙 재분석(임무 3)**: 안 했다. 말뭉치가 127회차 수집분 그대로고, `selfloop` 이 이미 그 말뭉치로 제목 분석을 돌렸다(코픽스 / blog 탭 상위 30개). 같은 말뭉치는 같은 값이 나온다.
- **새 도구 조사(임무 5)**: **못 했다.** 회차 예산(25분)을 ①의 36장 실측과 루프 두 바퀴(555초+404초)에 다 썼다. 127·128 로 **세 회차 연속**이다 → tools-wanted.md 에 올린다.
- **숏폼 생산(임무 6)**: 안 했다. 계기판 '올린 숏폼 수'는 11/10 으로 이미 채워져 있고, 12편째도 private 이면 조회로 아무것도 못 잰다. 재고를 늘리는 자리는 생산기(improve)다.

## 2026-09-26 04:4x (loop 131~132회차)

방문: 새벽 4:19 기준 오늘 5, 전체 11,245(어제보다 +6). 어제 하루치는 53(9/25 23:56).
하루가 4시간밖에 안 지난 값이라 어제와 견주지 않는다 — 이 함정 때문에 health.py 의 성장 눈금을
'마지막 완결일'로 바꿨다(오늘 값은 가중치 0 참고용으로만 남겼다). 점수 2.88 → 1.41.

영상: 채널 총조회 3,545 → 3,547(1.6시간에 +2). 최근 10편은 여전히 전부 private 라
썸네일 조정 효과는 조회로 못 잰다. 이건 사장님만 풀 수 있는 항목이다(공개 전환).

썸네일: 한 회차 안에서 long.contrast 를 닫았다가 물렸다. 자세한 것은 loop.py RULE['contrast'] 주석.
요지 — 닫히긴 닫혔는데 닫는 방식이 배경을 흑백으로 만드는 것이었고, 그러면 차트가 하려던 말이 사라진다.
하루치로 단정하지 않는다: 방문·조회 둘 다 움직임이 한 자릿수라 이 회차만으로는 오르내림을 말할 수 없다.

### 이 회차가 25분을 넘긴 이유 (구조 문제, 기록만 해 둔다)
loop.py 한 바퀴가 400~600초다. 그런데 firemap-loop 2)는 "고친 뒤 한 번 더 돌려 줄었는지
숫자로 확인한다"를 요구한다. 손잡이를 바꾸면 그 값으로 샘플을 다시 그리는 것은 그 회차 끝이고
재는 것은 다음 회차라, 실제로는 세 바퀴(처음·바꾸고·재고)가 필요하다 — 그것만 25~30분이다.
이번 회차는 그래서 1시간이 걸렸다. 고칠 길 두 가지: (가) 손잡이를 바꾼 뒤 그 자리에서
다시 그리고 재도록 loop.py 를 고친다(한 바퀴로 끝난다), (나) 예산을 40분으로 올린다.
(가)가 맞아 보이지만 tune_bands 와 순서가 얽혀 있어 한 회차를 통째로 써야 한다.
